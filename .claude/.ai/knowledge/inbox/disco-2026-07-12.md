---
tarefa: limpeza de disco C:
data: 2026-07-12
estado: concluido
---

# Limpeza de disco C: — 2026-07-12

## Situação encontrada
- Livre antes: **14,91 GB** (de 119 GB, 88% usado).
- Meta pedida: >=25 GB livres.
- Escopo autorizado (só isto, nunca código/config/dados): gradle caches `.gradle/caches` >7 dias,
  pasta TEMP do utilizador, gravações de vídeo E2E >2 dias.

## O que foi encontrado por categoria
- **Gradle caches >7 dias:** só existiam 4 ficheiros (210 KB) em `android/.gradle/` do próprio
  projeto (`resourceHashesCache.bin`, `buildOutputCleanup/cache.properties`, `file-system.probe`,
  `kotlin/errors/*.log`). `~/.gradle/caches` global (danil e hermes) estava **vazio**.
- **Pasta TEMP:** `C:\Users\danil\AppData\Local\Temp` e `C:\Users\hermes\AppData\Local\Temp`
  tinham poucos MB no total (node-compile-cache, logs adb, ficheiros de sessão do Claude) —
  não é onde o espaço do disco está a ser consumido.
- **Vídeos E2E:** `.claude/testes-e2e` tem 99 vídeos (~97 MB) mas **nenhum com mais de 2 dias**
  (loop de testes está ativo e a gerar gravações recentes continuamente) — nada elegível para apagar.

## Ações aplicadas
1. Apagados os 4 ficheiros de gradle cache >7 dias (210 KB).
2. Limpa a pasta TEMP de ambos os utilizadores (danil: 135→9 itens; hermes: 165→122 itens —
   os restantes estão em uso por processos ativos e foram ignorados, sem erros).
3. Nenhum vídeo E2E apagado (nenhum qualificava pela regra de 2 dias).

## Resultado
- Livre depois: **14,92 GB** (ganho de ~13,5 MB).
- **Meta de 25 GB NÃO atingida** — as três categorias autorizadas (gradle cache, TEMP, vídeos E2E
  recentes) já estavam praticamente vazias/pequenas neste ambiente; o espaço ocupado (~104 GB em
  uso) está noutro lado (ex.: `AppData\Local` de danil tem subpastas grandes que o scan não
  concluiu a tempo — não investigado, fora do escopo autorizado desta tarefa curta).
- Nada em código, configuração ou dados foi tocado.

## Recomendação para próxima tarefa (fora do escopo desta)
Se libertar 25 GB continuar a ser necessário, precisa de uma tarefa dedicada (mais tempo) para
auditar `C:\Users\danil\AppData\Local\*` e identificar as pastas grandes reais (ex. caches de
Android Studio/SDK, Chrome, Docker, node_modules antigos) — o scan recursivo completo de
`AppData\Local` não terminou dentro do limite de 10 min desta tarefa.
