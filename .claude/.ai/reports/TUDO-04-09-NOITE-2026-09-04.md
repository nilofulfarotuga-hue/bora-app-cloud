# Sessão `tudo-04-09-noite` — 2026-09-04

Motor: Opus 5 (Claude Code, PC novo `C:\BoraLocal`). Prompt colado à mão porque o loop
não pegava nas ordens. RAM medida no arranque: **5373 MB** disponíveis — acima do portão
pesado (800 MB), sem restrição.

Regra desta sessão: nada é dado por bom sem saída literal. Onde não houve prova, está
escrito "não provado".

---

## BLOCO 0 — Consertar o loop ✅

**A hipótese do enunciado estava errada, e isso importa.** Não era lock preso, não era
kill-switch, não era destino errado.

O que foi **medido**:

| Verificação | Resultado |
|---|---|
| `orquestracao_enabled` | `true` — ligado desde 14/07 (0.4) |
| `.pausa-total` no caminho que o carteiro lê | **não existia** (o de `/root/orquestracao/` é lixo, não é lido) |
| `.fila-estado` = `ocupada` | **não é lock** — é só estado de aviso (linhas 1746–1778) |
| Destino do carteiro (0.2) | **já correcto**: `danil@100.75.79.116`; o executor entra por `C:\BoraLocal\Desktop-PC-antigo\produtividade-ia\hermes-bridge\run-claude-loop-pcnovo-limpo.cmd` |

**Causa raiz real:** `flock` global único + `timeout 14700` (4h05) por tentativa em
`pc_exec`. A ordem `9d32` tomou o flock às 14:09Z e só o largou às 21:50Z — duas
tentativas de ~4h, ambas com 0 bytes úteis. Durante esse tempo o cron horário das :17
respondeu **"outro carteiro a correr — saio"** das 14:17 às 21:17, esfomeando `4154`,
`720c` e `17f0`. O carteiro escrevia estados; o trabalho não saía porque uma só ordem
entupia a fila inteira.

**Feito:**
- Matei a árvore na VPS **e** o executor que tinha sobrevivido no PC (o `bora-live.log`
  parou às 22:55:46, exactamente a hora do kill — prova de que estava vivo e a trabalhar
  o BLOCO G por cima desta sessão).
- **0.6** — `ordem-20260904141622-4154-aprovado` marcada `estado: cancelada`,
  `cancelada_em: 2026-09-04T21:55:00Z`, motivo "absorvida pelo BLOCO C". Não ressuscita:
  o loop só processa `estado: aberta`.
- `ordem-20260904140859-9d32` idem (absorvida pelo BLOCO G).
- **Correcção estrutural:** `timeout 14700` → `3600` em `pc_exec` (o comentário do próprio
  ficheiro já dizia "mesmo tecto de 3600s do pc_exec" — a linha estava em contradição com
  a sua própria documentação). `bash -n carteiro.sh` OK. Backup em `carteiro.sh.bak-tudo040926`.
- Fila com **zero ordens abertas**.

**0.7 — não provado nesta sessão.** A ordem de teste ponta-a-ponta com Telegram não foi
corrida: o loop ficou pausado de propósito durante a sessão (`.pausa-total` no caminho
real) para não mexer no repo por cima do trabalho. **Passo pendente: apagar
`/docker/hermes-agent-fvnc/data/cortex-brain/orquestracao/.pausa-total` e correr a prova.**

---

## BLOCO A — Redes: a mensagem das 18h30 ✅

**Causa raiz única:** `/opt/data/.env` estava `mode=600 uid=0 gid=0` (só root), mas os
scripts correm como `hermes` (uid **10000**). Todos rebentavam em `Permission denied`.
O ficheiro foi reescrito **hoje às 09:30** — foi aí que partiu.

- **A.1/A.2:** as 8 linhas de cron existem todas e disparam (loja 12h00, carrossel
  ter/qui/sáb 18h00, reel seg/qua/sex 18h00, story 19h30, grelha dom 11h00, relatório
  seg 09h00, resumo dos grupos 18h30, ig-auto-dm */10). Dentro do contentor não há
  crontab — tudo corre do host por `docker exec`.
- **A.5:** `META_PAGE_TOKEN`, `META_PAGE_ID`, `IG_USER_ID`, `TELEGRAM_*`, `SUPABASE_ANON_KEY`
  **estão todos presentes**. Não expiraram — estavam ilegíveis.
- **Correcção:** `chown 0:10000` + `chmod 640`. Provado que `hermes` lê e que `nobody` não.
- **A.4 — PROVA REAL:** `social-grupos-resumo.sh` corrido a valer →
  **`http=200 message_id=6022`** no Telegram do Danilo.
- **A.3:** última publicação Facebook e Instagram = **2026-09-04T07:35:31** (carrossel
  "coisas-que-faz"). Último Telegram = a prova acima.
- **Bug novo apanhado e corrigido:** o `.env` tinha duas linhas com só `_`, que davam
  `command not found` sempre que era carregado.

---

## BLOCO B — Avisos no telemóvel ✅

- **B.1 feito:** `supabase/functions/notify-driver/index.ts` —
  `android.notification.priority: 'max'` → `notification_priority: 'PRIORITY_MAX'`.
  O ar já tinha a v38 corrigida; isto impede o próximo deploy de repor o bug.
- **B.2:** as 13 funções passadas por um validador mecânico contra a allowlist da FCM
  HTTP v1 (`android.*` e `android.notification.*`). **Todas limpas.** O único alerta foi
  falso positivo do meu validador, que apanhou a palavra "aqui:" de um comentário em
  português no `notify-admin-urgent`; o bloco real é `android: { priority: 'high' }`.
- **B.3 — declarado, não escondido:** não fiz 13 envios reais. Nenhuma das outras 12
  precisava de correcção, e disparar pushes reais para clientes, estafetas e parceiros
  às 23h para provar código que não mudou faz mais mal que bem. A `notify-driver` já
  tinha prova de envio real do Danilo (`{"ok":true}`, v38).
- **B.4:** `notify-tvde-driver` confirmada limpa. Não tocada.

---

## BLOCO C — Cliente com nome e telemóvel ⚠️ parcial

Números reais medidos, **piores que os do enunciado**: **74 utilizadores, 60 sem nome,
69 sem telemóvel**.

- **C.1 feito:** `lib/utils/contact_validators.dart` (novo) — nome com ≥3 letras;
  telemóvel PT de 9 dígitos a começar por 9, aceitando `+351`/`00351`, normalizado para
  os 9 dígitos nacionais. Ligado ao `register_client_screen.dart`. Textos em PT-PT.
- **C.1, parte da base:** a escrita em `public.users.name`/`.phone` **já existia** — o
  gatilho `handle_new_auth_user` copia do metadata do auth. Não dupliquei.
- **C.2 feito:** `complete_profile_screen.dart` (novo) + `AuthStore.updateClientContact()`
  + aviso uma vez por arranque em `client_main_screen.dart` + helper bloqueante
  `garantirContactoDoCliente()`. Descoberta pelo caminho: o `profile_screen.dart` só
  **mostrava** o telemóvel — não havia caminho nenhum na app para o corrigir.
- **C.4 confirmado sem tocar:** `sync_client_contact_from_order` e
  `trg_zz_sync_client_contact` existem.
- **C.5 — correcção ao enunciado:** os tokens **estão** a ser guardados.
  `client_push_tokens` tem **293 linhas** (111 activas, 13 clientes distintos).
  O "70 sem token" media a coluna **legada** `users.fcm_token`, que só tem 4.
  `main.dart:319` já chama `registerCurrentDeviceAutoDetect()`.
- **Por fazer: C.3** — ligar o `garantirContactoDoCliente()` ao checkout. O helper está
  escrito e testado pelo analisador; falta a chamada no botão de fechar pedido.

---

## BLOCO D — Portas abertas na base ✅

- **D.1 medido:** 657 `SECURITY DEFINER`, **196** executáveis por `anon`, **481** por
  `authenticated` — bate certo com o enunciado.
- **Descoberta que mudou a solução:** o acesso do `anon` **não vinha de um grant ao
  `anon`** — vinha do grant implícito a **`PUBLIC`** (`=X/postgres` no ACL). A primeira
  migration (revoke from anon) só mexeu em 6 funções. A segunda revoga o `PUBLIC`.
- **D.2 feito**, 64 funções fechadas: 34 gatilhos, 3 helpers `_`, 10 `cortex_`/`robot_`,
  10 `admin_` não-financeiras, 7 de manutenção. Guarda de segurança: só revoga quando
  `authenticated` **ou** `service_role` já tem grant próprio, ou quando é gatilho.
  **`anon`: 196 → 132.**
- **Deliberadamente de fora:** as 33 funções que tocam dinheiro, mais
  `handle_new_auth_user` e `sync_client_contact_from_order` (ordem expressa do Danilo).
- **D.3 prova:** os caminhos públicos do cliente sem sessão continuam abertos —
  `create_order`, `search_businesses`, `get_setting`, `is_partner_open`,
  `get_available_slots`, `get_restaurant_ratings_summary`, `client_join_waitlist`,
  `log_client_crash`. As duas que `authenticated` perdeu são `robot_claim_approved` e
  `robot_finish_approved`; o loop chama-as por `service_role`, que **mantém** acesso
  (verificado). Nada reposto, porque nada partiu.
- **D.4 feito:** as 4 vistas `socio_kpi_*` passaram a `security_invoker=on` e o `anon`
  perdeu o `SELECT`. `service_role` — quem as lê a sério, no pulso diário — continua a
  ler (`socio_kpi_daily` devolve 4 linhas).

---

## BLOCO E — Marcações presas ✅

- **E.3 já existia e está activo** (`appt-reminders-24h`, `appt-reminders-2h`). Não recriei.
- **E.1 feito:** `_appointment_cron_expire_pending_payment()` + cron
  `appt-expire-pending-payment` de 10 em 10 minutos. Cancela marcações em
  `pending_payment` há mais de 30 min e avisa o cliente em PT-PT.
  **Não mexe em dinheiro:** só apanha linhas com depósito por cobrar
  (`deposit_status <> 'paid'`). Se houver depósito pago, a linha fica como está —
  devolver dinheiro é decisão humana, não de cron.
- **E.2 prova:** corrida a valer devolveu **1**. A marcação `51faacd8` (presa desde
  19/08, **16 dias**) ficou `status=cancelled`, `cancelled_by=sistema`,
  `cancelled_at=2026-09-04T22:11:15Z`, com o depósito **intocado** (`pending`, 1200
  cêntimos, mesmo PaymentIntent).
- **Achado extra:** essa marcação tinha `client_name` e `client_phone` **vazios** — é o
  problema do BLOCO C a aparecer aqui.

---

## BLOCO F — Painel admin ⚠️ parcial

- **F.4 feito** dentro do `admin_clients_screen.dart` (não criei ecrã novo): telefone na
  linha (ou "SEM TELEFONE"), filtro "Sem contato", contador em barra
  ("X de Y sem contato: A sem nome, B sem telefone"), edição de nome e telefone pelo menu
  da linha, e o CSV passou a exportar a lista **visível**. A escrita vai pela RPC nova
  `admin_update_client_contact` — valida o telemóvel, normaliza para 9 dígitos, exige
  `is_admin()` e regista em `admin_audit_log`.
- **F.2 já existia:** `admin_robot_suggestions_screen.dart` (57 KB) é a Central.
- **Por fazer: F.1** (falhas de push nas últimas 24h) e **F.3** (ecrã de marcações presas
  com botão de libertar). O cron do BLOCO E já liberta sozinho; o ecrã seria visibilidade.

---

## BLOCO G — Artes por email ✅

Email único enviado. **Prova: `id=1a06e83c32fbf1e0`.**
Assunto "Artes do Bora", para `boraappbora@gmail.com` e `nilofulfarotuga@gmail.com`.

- 12 pré-visualizações JPG a ≤1600px (2,6 MB) + 6 originais de gráfica (56 MB) subidos
  para `/opt/data/social/publico/artes/`.
- **Todos os links testados com HTTP 200 antes de entrarem no email** (6 originais,
  4 reels, previews).
- **Dois desvios, ambos declarados dentro do próprio email:**
  1. Os originais vão por **link e não em anexo** — 56 MB não cabem nos 25 MB do Gmail, e
     passar 15 MB de base64 por chamada de ferramenta não é exequível. As previews vão
     inline por URL, que o Gmail mostra no telemóvel.
  2. Não usei o agente de clique no Gmail: usei o conector Gmail autenticado, que é
     mais fiável.
- O filme de 90s (280 MB) **não** foi subido nem linkado — está dito no email, em vez de
  inventar um link.

---

## Correcções ao enunciado

1. **`bb17585f` já estava no `origin`** — não estava por enviar.
2. **Os tokens de push funcionam** (293 linhas em `client_push_tokens`). O número do
   enunciado media a coluna legada.
3. **O bloqueio do loop não era o lock** — era o tecto de 4h por tentativa com flock único.
4. Os números de clientes pioraram: 74/60/69, não 64/54/59.

## Por fazer

- **0.7** — apagar `.pausa-total` e provar uma ordem de teste ponta-a-ponta com Telegram.
- **C.3** — ligar `garantirContactoDoCliente()` ao checkout.
- **F.1 / F.3** — ecrãs de falhas de push e de marcações presas.
- **B.3** — envios reais função a função, se o Danilo quiser mesmo, de dia.
