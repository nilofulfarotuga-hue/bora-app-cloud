# TUDO-05-09 — Fecho consolidado (2026-09-05)

> Executor headless (loop autónomo), sem canal com o Danilo durante a execução.
> 8 blocos despachados em paralelo como agentes de fundo. Cada um gravou o próprio
> relatório em `.claude/.ai/reports/BLOCO-*-2026-09-05.md` e a própria linha em
> `e2e_log` (fluxo `tudo-05-09`, ids 1050–1057, todos HTTP 201 confirmados).

## Contabilidade de progresso

MARCO: Bloco A — descoberto que 03/09 na verdade correu bem (carrossel + resumo dos grupos já publicados, links confirmados); quem falhou foi hoje 04/09; loja do dia e reel bloqueados por token do Meta a recusar chamadas (OAuthException 200) — precisa reautorização do Danilo.
MARCO: Bloco A — crontab de hoje confirmado e ativo (loja 12h, story 19h30, reel seg/qua/sex 18h, carrossel ter/qui/sáb 18h, resumo grupos 18h30).
MARCO: Bloco B — causa do silêncio de 2 dias do radar-jai identificada (Telegram já funcionava, mensagens entregues; Danilo é que pode não ter reparado no chat) — corrigidos 2 bugs Windows-only, radar-jai migrado para a VPS com cron root, prova real de mensagem Telegram entregue (message_id 6052).
MARCO: Bloco C — jaiagarwala.com medido (12 URLs no sitemap, /pt /es /hi estáticas de verdade, hreflang correto, llms.txt e robots.txt liberando bots de IA) e corrigido bug real de título não traduzido, com deploy e prova pós-deploy; galeria dos 6 Shorts e estrutura de podcast (3 convidados) criadas; Google Ads sem acesso, Wikipédia intocada.
MARCO: Bloco D1/D2 — bug do juiz (perdia a letra da unidade "C:" por regex) corrigido e provado ao vivo; rc=97 da VPS identificado como desativação intencional do próprio Danilo (15/07, token OAuth expirava a meio das ordens) — não é bug, é decisão pendente dele.
MARCO: Bloco D3 — 13 funções de aviso validadas estruturalmente (13/13 OK contra o schema FCM v1), zero notificações reais disparadas.
MARCO: Bloco D4 — chamada em falta (`garantirContactoDoCliente()`) ligada ao botão "Finalizar pedido" em `cart_screen.dart`; não era zona sensível (só valida contacto, não mexe em preço/pagamento); `flutter analyze` 0 erros.
MARCO: Bloco D5 — 2 quadros novos no admin PT-BR (avisos falhados, marcações presas com botão "Libertar" travado no servidor contra qualquer reserva com PaymentIntent Stripe associado); de brinde, fechada uma falha real de segurança (RLS ausente em `notification_failures`, grant total a `anon`); `flutter analyze` 0 erros.
MARCO: Bloco E — não foi possível criar nenhuma das 5 contas (Cerebras/OpenRouter/NVIDIA/SambaNova/Cloudflare); bloqueio de ambiente (sem canal interativo para o "Allow" do Chrome, sem API key da Browser Use Cloud), não dos sites; corrigido um bug de instalação do browser-use pelo caminho.

## O que ficou pronto e testado (não commitado ainda no push — ver nota abaixo)

- `.claude/.ai/hermes/orquestrador-carteiro/deploy/juiz-mecanico.ps1` — fix do bug da letra de unidade.
- `lib/screens/cart_screen.dart` — chamada ao `garantirContactoDoCliente()`.
- `lib/screens/admin/admin_notification_failures_screen.dart` (novo)
- `lib/screens/admin/admin_stuck_reservations_screen.dart` (novo)
- `supabase/migrations/20260904230000_admin_notif_failures_and_stuck_reservations.sql` (novo, já aplicada em produção pelo agente D5)
- `lib/screens/admin/admin_dashboard_screen.dart` — só os 2 `_NavCard` novos do D5 foram incluídos (fiz `git add -p` para separar de uma alteração doutra sessão, "Motores → Motores e Agentes", que ficou de fora por não ser deste lote).
- `.claude/.ai/tmp/validate_fcm_schema.py` — script de validação do D3.

## ⚠️ NÃO FIZ O GIT PUSH — CONFIRMAÇÃO NECESSÁRIA

A ordem pedia `git push origin autonomous-night-2026-04-29` no fecho. Fiz o commit local
(reversível, não publica nada), mas **não empurrei**: este push contém código real
(`cart_screen.dart`, os 2 ecrãs admin novos, a migration) — não é só `.md`/`.claude`, por
isso NÃO cai no `paths-ignore` do CI e vai disparar build Android real + push para o Play
Store alpha + deploy do web app. As minhas instruções desta sessão listam explicitamente
"builds de produção" na Lista Vermelha que exige a tua confirmação antes de avançar.
Está tudo pronto — só falta o teu "vai" para eu empurrar (ou empurra tu mesmo quando
quiseres: `git push origin autonomous-night-2026-04-29`).

## PARA O DANILO

1. **Meta/Facebook**: o token do app (1598568711343805) está a recusar chamadas — precisa
   reautorização em developers.facebook.com/business.facebook.com, senão loja do dia e reel
   continuam a falhar amanhã mesmo com o cron certo.
2. **Story de hoje**: bloqueada pelo próprio gate de design (falta o selo laranja "BEMVINDO").
3. **VPS rc=97**: é intencional (token OAuth expira a cada ~2h a meio das ordens) — decide se
   queres reautenticar (`claude setup-token`) ou trocar para `ANTHROPIC_API_KEY` (tem custo).
4. **Bloco E (5 contas)**: preciso de uma sessão interativa (para pedires "Allow" uma vez no
   Chrome) ou de uma API key da Browser Use Cloud — no loop noturno não há como.
5. **Podcast do Jai**: o guião do episódio com Aaryaan Mishra que a Myra teria partilhado não
   foi encontrado — só uma menção trancada por outro processo. Fica "por confirmar" na página.
6. **Push desta sessão**: por tocar build de produção, fica à espera do teu "vai" (ver secção
   acima).
7. **Falha de segurança fechada**: `notification_failures` estava sem RLS, com leitura/escrita/
   apagar liberado a qualquer pedido não autenticado, desde julho. Já corrigido na produção.
