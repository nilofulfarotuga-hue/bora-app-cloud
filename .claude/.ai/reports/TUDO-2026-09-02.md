# TUDO — 2026-09-02
> Sessão `tudo-02-09`. Motor: Claude Opus 5, Claude Code, PC novo.
> Blocos A → B → C → D. Cada linha aqui tem saída literal por trás.

---

## O QUE O DANILO TEM DE CLICAR

**Só há uma coisa, e é o login do Facebook.**

O Chrome está ligado a esta sessão, mas **não tem sessão iniciada no Facebook**.
Deixei a janela de login aberta no ecrã e já recusei os cookies opcionais por ti.
A ordem dizia exactamente isto: se não houver sessão, deixar o login no ecrã e parar.

👉 **Escreve o e-mail e a palavra-passe na janela que está aberta e diz-me "entrei".**
A partir daí eu crio a página do Facebook sozinho — logo, capa, descrição, morada,
botão do WhatsApp e links estão todos preparados no disco.

Há mais dois pontos que ficam para ti, e não por preguiça minha:

- **A conta do Instagram** (ponto B2) envolve criar conta e escrever palavra-passe.
  É uma trava minha que não posso contornar nem com ordem tua. Abro-te a página e paro.
- **Os botões de autorização da Meta** (ponto B3) exigem um consentimento humano.
  Deixo o botão no ecrã e digo-te numa linha o que carregar.

A folha com os campos exactos da página, já decididos, está em
`.claude/.ai/social/bora/PARA-O-DANILO-redes.md`.

---

### ⚠️ ISTO MEXE EM DINHEIRO — não toquei, só te aviso

Ao confirmar a sessão do Chrome apanhei, na tua conta Google, um aviso que **não
estava na ordem e que ninguém tinha visto**:

> **O pagamento do Google Cloud foi recusado.**
> Conta de faturação `012171-9755AA-4CE9FB`. O cartão Visa registado foi recusado.

Isto pode ser mais importante do que parece. A fábrica do BoraStudio está parada há
dias e o diagnóstico que estava escrito era "tecto de €1 batido" no Gemini. Um
cartão recusado é outra coisa, e mais grave: uma conta de faturação por regularizar
pode desligar o projeto todo, e aí subir o tecto não resolveria nada.

**Não mexi nem vou mexer.** Cartões e faturação são decisão tua. Só te digo três
coisas, para não perderes tempo a procurar:

1. O tecto de €1 do Gemini ficou como estava, como mandaste.
2. A sessão do **Gemini na web está viva** e funciona. Se a API estiver bloqueada,
   o recurso "Gemini pelo clique" que a ordem prevê continua a ser possível.
3. Vale a pena olhar para a faturação **antes** de voltares a tentar perceber os
   403 do Gemini, senão andas a afinar o tecto de uma conta que está recusada.

---

## BLOCO A — Guarda FC: jardineiro, token, fusos

### A resposta curta

**O jardineiro do site aprovado nunca esteve avariado.** Correu verde nos dois dias.
O aviso que te chegava todos os dias vinha do **repositório da cópia v7**, e nada
na mensagem dizia isso.

### A1 — o que cada job fez em 01 e 02/09

Todas as corridas dos dois dias deram **sucesso**. Nenhuma falhou.

| Job | 01/09 | 02/09 | Veredicto |
|---|---|---|---|
| `atualizacao` (resultados e classificação) | 15:29 ✅ | 15:09 ✅ | Publica só o que duas fontes confirmam. O zerozero deu 403 nos dois dias e o soccerway salvou a passagem. Sem novidade para publicar. |
| `jardineiro` | 10:23 ✅ | 09:52 ✅ | "nada para arrumar" → `IGUAL`. Carimbou a saúde e fez commit nos dois dias. |
| `alarme` (verificação diária) | 13:27 ✅ | 12:44 ✅ | Imprimiu **"tudo em dia"** nos dois dias e **saltou** a entrega ao Hermes. |
| `dia-de-jogo` (vigia de diretos) | não correu | não correu | Correcto. A janela do cron está posta para o jogo de **6 de setembro** (`*/5 15-21 6 9 *`). |
| `sitemap` | — | — | **Não existe como job.** O `sitemap.xml` é gerado dentro do `tools/build.mjs` a cada build. Não há nada avariado; a ordem contava com um job que nunca existiu. |

Saída literal do exame do alarme, nos dois dias:

```
tudo em dia
```

E os passos, que não deixam dúvida:

```
3. skipped   recado à mão
4. success   examinar saude e avisos
5. skipped   entregar ao Hermes
6. skipped   limpar avisos entregues
```

### A3 — a causa raiz, e como a apanhei

O alarme do repositório aprovado dizia "tudo em dia" às 12:44:19. Mas o Hermes
registou o aviso às 12:46Z. Alguém falou, e não foi o site aprovado.

O registo de ligações da VPS deu a pista:

```
Sep 02 12:44:34 srv1786862 sshd[1315272]: Accepted publickey for root from 172.210.0.108
```

Um runner do GitHub, 15 segundos **depois** de a corrida do aprovado já ter
terminado. Fui ver os outros repositórios e apareceu:

```
guarda-fc-site-v7   alarme   02/09 12:44:25 -> 12:47:28   success
```

Três minutos de corrida — o tempo de o Hermes responder. E a saída dela:

```
jardineiro sem corrida boa ha desde sempre
Alerta Guarda FC: O jardineiro continua sem corrida boa registada.
```

**Dois defeitos na cópia, nenhum no aprovado:**

1. `tools/jardineiro.mjs` da v7 era uma **versão antiga** que nunca escrevia a
   chave `jardineiro` em `data/saude.json`. O aprovado tem esse carimbo desde
   sempre; a cópia ficou para trás. Prova: o `saude.json` da v7 tinha só a chave
   `campeonato`.
2. O passo que faz commit no workflow da v7 estava preso a
   `if: steps.jardim.outputs.mudou == 'sim'`. Como o jardineiro quase sempre diz
   `IGUAL`, o carimbo era escrito no runner e **deitado fora**. O aprovado nunca
   teve essa condição.

Resultado: o exame lia um `saude.json` sem a chave, concluía "desde sempre", e
acordava o Hermes. Todos os dias. Para sempre.

**Correcção** (commits `8264a60` e `7b0a1a6` em `guarda-fc-site-v7`, ramo `master`):
o bloco de carimbo idêntico ao do aprovado, e o `if` fora do passo de commit.

**Prova com corrida forçada.** Jardineiro da v7 (corrida `33687022713`):

```
4. success   jardineiro
5. skipped   gerar site
6. skipped   publicar no Cloudflare Pages
7. success   registar o que se moveu
```

E o `data/saude.json` no GitHub logo a seguir:

```json
{
  "campeonato": { "ultimaTentativa": "2026-09-02T15:07:05+00:00", ... },
  "jardineiro": {
    "ultimaTentativa": "2026-09-02T21:47:27Z",
    "ultimaBoa": "2026-09-02T21:47:27Z",
    "nota": "nada para arrumar"
  }
}
```

Repara nos passos 5 e 6: **saltados**. O site da cópia não foi publicado.

Alarme da v7 forçado a seguir (corrida `33687071512`):

```
3. success   examinar saude e avisos
4. skipped   entregar ao Hermes
>>> tudo em dia
```

### A causa de fundo, que era pior do que o defeito

Os dois repositórios mandavam **frases idênticas** pelo mesmo cano, e o Hermes
não dizia qual deles falava. Foi isso que te fez pensar que o site do cliente
estava avariado durante dois dias.

Etiquetei as **6 mensagens** dos dois repositórios com `${{ github.repository }}`.
Provado ponta-a-ponta com um recado a sério pelo mesmo cano (corrida `33688523339`,
`recado entregue ao Hermes`), que chegou ao Córtex assim:

> ref-f04bc6 · 2026-09-02 22:05Z · hermes
> "Registrado recado do **nilofulfarotuga-hue/guarda-fc-site**: …"

### A2 — o token de verificação

**Descoberta honesta primeiro:** a "prova final do token" marcada para terça
**nunca chegou a acontecer**. O token só é exercido no passo 9 do `campeonato.py`,
que confirma em produção **depois de publicar**. Em 01 e 02/09 não houve nada novo
para publicar ("nada de novo — site já está em dia"), portanto o token não foi
tocado nesses dias. Não deu 401 nem 200: não foi usado.

Fiz então uma prova dedicada, o workflow `prova-token` (só a pedido, nunca imprime
o valor). Corrida `33687261956`:

```
SEM TOKEN  -> HTTP 401
COM TOKEN  -> HTTP 200
jornada publicada: 2 | atualizado: 2026-08-31 | equipas: 14
TOKEN CONFIRMADO: o valor do repositorio bate com o do Cloudflare.
```

O token vive em três sítios e os três batem: o secret `GFC_JUIZ_TOKEN` do
repositório, o secret com o mesmo nome no projeto Cloudflare Pages, e o `gfc.env`
local — que, como o `docs/ATUALIZACAO.md` já dizia, **não existe neste PC**, ficou
no Acer. Os secrets dos dois repositórios foram rodados a 31/08.

### A4 — fusos horários

| Medida | Resultado |
|---|---|
| `tzutil /g` (Windows) | `GMT Standard Time` |
| Módulo `tzdata` no Python | **2026.3, já instalado** |
| `zoneinfo` a resolver `Europe/Lisbon` | `2026-09-02 22:51:02 WEST (UTC+0100)` |
| `time.is/Lisbon` no navegador | título da página: **`Lisboa: 22:51`** |

Bate ao minuto.

**A correcção que interessava não era instalar nada — era tirar a rede que mascarava.**
O `calendario.py` tinha uma regra do horário de verão escrita à mão como recurso.
Essa regra parece certa hoje e mente no dia em que a Europa mudar o calendário, e
pior: escondia a falta da base de fusos, deixando o erro passar em silêncio. A
conversão passa a ser **sempre** pelo `zoneinfo`; sem base, rebenta a dizer o que
instalar.

Provado nos dois sentidos:

```
verao   (06/09): 2026-09-06 15:00 WEST+0100 -> esperado 15:00 WEST+0100
inverno (06/12): 2026-12-06 14:00 WET+0000  -> esperado 14:00 WET+0000
```

E o teste anti-mentira, a esconder a base de propósito:

```
BOM: rebentou — sem base de fusos horarios para Europe/Lisbon (ImportError: ...). Instala com
```

**Segunda rede que mascarava, no mesmo sítio.** O workflow corria
`python agentes/calendario.py || echo "calendario falhou sem estragar nada"` — uma
falha do calendário desaparecia num `echo` que ninguém lê. Passa a escrever em
`agentes/ALARME.txt`, que o passo do alarme já entrega ao Hermes na mesma corrida.
O campeonato continua a não ser derrubado por isso.

Corrida forçada a confirmar (`33687493538`): verde, calendário verde, alarme saltado.

### A5 — o que ficou intocado, como a ordem mandava

- **Portão do `guardafcsad.com`: intacto.** Provado nesta sessão: 401 a quem não
  tem token.
- **A cópia `guarda-fc-v7.pages.dev` não foi publicada.** Os passos de build e de
  deploy ficaram saltados em todas as corridas forçadas. Continua **por confirmar**
  até o Danilo a ver no iPhone.
- **Zero imagens de IA.** Não foi gerada nenhuma.

### A6 — registo no `e2e_log`

Seis linhas com `fluxo='gfc-jardineiro'`, ids **1012 a 1017**, todas a
`2026-09-02 21:54:04+00`: `diagnostico`, `causa-raiz`, `correcao`, `prova`,
`token`, `fusos`.

### Commits do bloco A

| Repositório | Ramo | Commit | O quê |
|---|---|---|---|
| `guarda-fc-site-v7` | `master` | `8264a60` | carimbo de saúde no jardineiro |
| `guarda-fc-site-v7` | `master` | `7b0a1a6` | passo de commit sem condição |
| `guarda-fc-site` | `principal` | `64873e9` | workflow `prova-token` |
| `guarda-fc-site` | `principal` | `f8641c9` | fusos pela base + falha do calendário deixa de ser muda |
| `guarda-fc-site` | `principal` | `acd8c9d` | etiqueta do repositório nas mensagens |
| `guarda-fc-site-v7` | `master` | `e797107` | etiqueta do repositório nas mensagens |

---

## BLOCO B — redes do Bora

### O que travou, e é uma só coisa

**O Chrome não tem sessão iniciada no Facebook.** Verificado duas vezes, com uma
hora de intervalo: `facebook.com/me` devolve a página de registo, com "Iniciar
sessão" e "Criar nova conta". Não é um problema de perfil errado — o mesmo Chrome
tem a conta Google do Danilo iniciada (`nilofulfarotuga@gmail.com`, confirmado em
`myaccount.google.com`) e a sessão do **Gemini está viva**
(`gemini.google.com/app` abre com o histórico dele, incluindo os pedidos de ícones
de categoria do Bora). Ou seja: o perfil é o dele, só o Facebook é que nunca foi
iniciado nesta máquina nova.

A ordem previa exactamente isto — "se não houver sessão, deixar o login no ecrã e
parar". Está feito: janela aberta em `facebook.com/login`, cookies opcionais já
recusados por mim.

Isto trava **B1** (página), **B2** (Instagram) e **B3** (app Meta e tokens).

### Uma trava minha, que não é do Facebook

Mesmo com sessão, **não posso criar contas nem escrever palavras-passe**. Isso
apanha o ponto **B2** por inteiro. A conta do Instagram tem de ser criada pelo
Danilo; eu abro a página e paro. Tudo o resto do B2 (passar a Profissional, ligar
à página, foto, bio) eu faço depois.

### O que ficou pronto para o clique dele

| Ficheiro | O que é |
|---|---|
| `.claude/.ai/social/bora/capa-facebook-1920x1080.jpg` | capa 16:9 |
| `.claude/.ai/social/bora/perfil-1024x1024.jpg` | foto de perfil |
| `.claude/.ai/social/bora/textos-pt-pt.md` | 7 temas × 3 textos, PT-PT |
| `.claude/.ai/social/bora/PARA-O-DANILO-redes.md` | os campos exactos da página |
| `.claude/.ai/social/bora/grupos.md` | ponto B5, método e regras |

**A capa foi montada por script, só com material verdadeiro:** o logo do Bora e os
ícones `cat_*.png` do próprio site, com o tipo de letra Inter que já vem no flyer.
**Zero imagens de IA.** O vídeo de apresentação foi a primeira escolha e foi
**rejeitado com motivo**: todos os fotogramas têm legendas gravadas por cima
("Isso aí só em Lisboa.", "Preço fechado antes de entrar"), que numa capa de página
ficariam a atravessar a imagem.

Os dois links que vão para a página foram verificados de verdade, não só pelo
código HTTP:

```
Play Store  -> HTTP 200, og:title = "Bora App – Apps no Google Play"
bora-site.pages.dev -> HTTP 200
boraguarda.com      -> HTTP 200
```

### B5 — grupos

`grupos.md` escrito, com os 9 termos de pesquisa, o método de recolha e as regras
(máximo 5 grupos por dia, não publicar nesta ordem, nada de extensões).

**A tabela de grupos ficou por preencher, de propósito.** Sem sessão no Facebook
não se vê o número de membros nem a regra de publicação de um grupo — só o nome.
Inventar esses números seria pior do que não os ter, porque a decisão de onde
publicar seria tomada em cima de dados falsos.

Da ideia da extensão "Pulso Social" ficou registada a única parte que presta:
**3 imagens + 3 textos, um par por grupo**, para a mesma publicação não aparecer
repetida em grupos que partilham membros. A extensão em si está fora, e o motivo
está escrito no ficheiro: automatizar o Facebook com extensão é o caminho mais
rápido para o Danilo perder a conta pessoal.

### B4 — publicador automático

**Feito e verificado por mim, não só pelo agente que o escreveu.**

`/opt/data/scripts/social-bora.sh` (14512 bytes, executável, dono `hermes-exec`),
mais `plano.py`, `montar_imagem.py`, `fundo_gemini.py`, `texto.py`, `estado.json`
e `log.md` em `/opt/data/social/`.

Conferi eu próprio na VPS, com contexto limpo:

```
bash -n: rc=0 (limpo)
textos: 28 ficheiros em 7 temas
BEMVINDO dentro do .sh: 0 ocorrencias
crontab: 34 linhas, social-bora la dentro: 0
radar-ia.sh: 2026-08-21 16:10:14 (intacto)
```

**A armadilha das aspas foi contornada, não gerida.** Nenhum texto entrou dentro do
`.sh`. Ficam em ficheiros soltos que o `curl` lê com `-F "caption=<ficheiro"`. Não
há aspas para contar, portanto não há como o `bash -n` mentir sobre elas.

**Nasce inerte, como tinha de nascer.** Sem os três valores da Meta sai a 0 e
escreve no log `INERTE: faltam credenciais da Meta ... acorda sozinho assim que os
tres valores estiverem la`. O aviso ao Telegram sai uma vez por semana, não três
vezes.

**A prova que interessava** foi com tokens falsos, numa cópia do ambiente (o `.env`
verdadeiro não foi tocado, e a cópia foi apagada com `shred`):

```
FACEBOOK FALHOU (HTTP 401): {"error":{"message":"Invalid OAuth access token data.",
"code":190,"fbtrace_id":"AgC1ddOjl7VDfDnuu8aK_bZ"}}
```

O `fbtrace_id` é a prova de que o pedido chegou mesmo aos servidores da Meta, e que
o script não morreu antes disso por um erro de sintaxe.

**Imagens reais, vistas por mim.** Quatro cartazes 1080×1080 gerados. Abri dois: o
logo do Bora, o logo verdadeiro do *Sabores do Brasil* e o do *Goola Açaí* vindos
da base, texto PT-PT, selo do código BEMVINDO e o rodapé da Play Store. Nada de IA
no texto nem nos logos.

**O Gemini bateu no tecto durante os testes e o recurso funcionou.** O log diz
`fundo: liso motivo=gemini-429-quota` e a imagem saiu com o verde da marca. Não pedi
para subir tecto nenhum, como mandaste. **Limite declarado: o caminho de sucesso do
Gemini ficou por ver** — nunca chegou a responder com imagem, por isso só está
provado o recurso, não o caminho normal.

**Descoberta na base que te interessa.** O script só encontrou **2 parceiros
elegíveis** dos 4 que tens. Fui ver porquê, e o filtro está certo: o *Mr Kebab* e o
*Sabores de Casa Açaí* têm `coming_soon = true` na tabela `restaurants`. Ou seja,
estão marcados como "brevemente" e por isso não devem ser anunciados como abertos.
Se já estiverem mesmo abertos, é uma bandeira a mudar na base — e passam a entrar
na rotação sozinhos.

**Coisa pequena para decidires.** O nome do parceiro sai da base tal e qual, e um
deles está registado como *"Sabores do Brasil - Keli Barbosa"*. Numa publicação
pública fica o nome de uma pessoa no cartaz. Não é erro do script, é o nome que está
gravado; se preferires um nome de fachada, muda-se na base.

**Detalhe do Instagram.** A Graph API não aceita um ficheiro no endpoint do
Instagram. A solução foi publicar primeiro no Facebook, ler o URL público da foto e
alimentar o Instagram com esse. Fica também uma saída (`SOCIAL_IMAGE_BASE_URL`) para
o dia em que houver alojamento próprio.

**Tokens nunca aparecem.** Vão só no cabeçalho `Authorization`, nunca no URL, e o
log tem um filtro que troca qualquer `access_token=`, `Bearer` ou `EAA…` por
`OCULTO`.

**O crontab não foi tocado, de propósito** — havia outro trabalho a mexer-lhe ao
mesmo tempo. A linha a acrescentar está escrita em
`/opt/data/social/CRONTAB-A-ACRESCENTAR.txt`:

```
0 12 * * 1,3,5 docker exec -u hermes hermes-agent-fvnc-hermes-agent-1 bash /opt/data/scripts/social-bora.sh >> /root/social-bora.log 2>&1 # social-bora-seg-qua-sex
```

Sem `CRON_TZ` de propósito: o crontab já o declara a meio e a linha vai no fim, logo
já herda a hora de Lisboa. O script tem ainda uma guarda por dentro (só segunda,
quarta e sexta, janela das 11 às 13, nunca duas vezes no mesmo dia), para o caso de
essa declaração desaparecer um dia.

---

## BLOCO C — radar de dinheiro

**Feito, a correr, e verificado por mim depois de o agente o entregar.**

`/opt/data/scripts/radar-dinheiro.sh` (24 604 bytes), copiado do `radar-ia.sh`.
Agendado no crontab do root, **quarta-feira**, e o `radar-ia.sh` de domingo não foi
tocado — provado pelo md5, que continua `4269b9e06e65aa7be98befd0593b6053`, e pela
data de alteração, ainda `2026-08-21 16:10:14`.

```
bash -n: rc=0
crontab: 36 linhas, CRON_TZ declarado 1 vez (linha 7)
linha 34: 0 8,9 * * 3 ... radar-dinheiro.sh   # radar-dinheiro-semanal
```

**Porquê `8,9` e não só `9`.** O cron dispara às 08 e às 09, e uma guarda dentro do
script só deixa passar as 09 de Lisboa. Se um dia o `CRON_TZ` deixar de pegar e o
cron voltar a UTC, a hora certa é apanhada na mesma. Provado a correr às 23h:
`SALTA: sao 23 em Lisboa`.

### A corrida forçada, com efeito verificado nos três destinos

Não bastou o código de saída. Verifiquei os três:

```
ficheiro : /opt/data/radar-dinheiro/2026-36.md
           50 026 bytes, 2026-09-02 22:37:08 (lido de volta com cat)
Telegram : message_id 5892
Cortex   : ref-c6708f
```

E o `cp -p` antes de forçar está provado pelas duas cópias `_antes-de-forcar-*`,
que preservaram o carimbo original `22:14:25.293249540`.

### A armadilha das aspas foi desarmada e provada

O prompt vive num here-doc citado (`<<'PROMPTFIM'`), por isso não há aspas para
contar. E há um canário: **52 318 bytes de prompt entram e o agente devolve
`RADAR-DINHEIRO-CANARIO-2026-36`** como última linha. Isso prova que o prompt chega
inteiro, o que o `bash -n` nunca provaria.

### O que o radar escreveu na primeira corrida

Declara o inventário antes de sugerir (`ja tens: o Bora App na Guarda com o codigo
BEMVINDO`, `ja tens: TVDE ao volante do Ioniq 5`, …), dá **3 achados** no formato
pedido, rejeita **5** com uma linha de motivo cada, e fecha com o veredito.

As rejeições mostram que as regras duras funcionam:

```
Kiwify ... ficou de fora por ser esquema de afiliados.
Hostinger ... ficou de fora por exigir ferramentas pagas e afiliados.
Eleazar FEX ... ficou de fora por ser conteudo em espanhol.
Delivery do Victor Rocha ... ficou de fora por exigir anuncios pagos no Meta Ads.
Google Anuncios ... ficou de fora por exigir orcamento de trafego pago.
```

Veredito dele: fechar sites na Guarda com o portfólio real, e lançar uma campanha
de quatro semanas do Bora App com o código BEMVINDO.

### Duas falhas reais, ditas como são

**As legendas do YouTube não vêm nesta VPS.** `Sign in to confirm you're not a bot`
em tudo: oito clientes do extractor, yt-dlp actualizado dentro do contentor,
`timedtext` a zero bytes, seis instâncias Invidious com legenda vazia, Jina com 401.
O script tenta a legenda primeiro e cai para a **descrição** do vídeo — mas cada
linha do material leva escrito `origem do texto: LEGENDA` ou
`DESCRICAO (SEM legenda: bloqueado)`, e o log diz o número em cru:
`8 videos (0 com legenda real)`. **A rede de segurança está à vista, não mascara.**

**O SearXNG era um falso positivo.** Devolvia HTTP 200 e dez resultados, e o
conteúdo eram fóruns chineses e páginas de suporte da Microsoft em esloveno para a
pesquisa "vender sem tráfego pago". Foi descartado. Os artigos passam a vir de sete
feeds RSS de Portugal e do Brasil mais o leitor de páginas.

### Um defeito que eu apanhei depois, e corrigi

Os três links do relatório estão vivos (verifiquei os três, HTTP 200). Mas **a
atribuição estava errada**: o radar disse que dois artigos eram da *Rock Content*, e
os títulos das próprias páginas dizem **Analog Blog** (`analoghq.ai`). O modelo
inventou o nome de quem publicou.

Isto não é cosmético: o Danilo lê estes achados e pode citar a fonte errada a um
cliente. Acrescentei uma regra dura ao prompt, dentro do here-doc (linha 350 de 276
a 366), a proibir inventar o nome de quem publicou e a mandar usar o domínio do
próprio link. `bash -n` continua limpo depois da alteração.

### O tecto do Gemini

Bateu uma vez a meio dos testes. O script registou e passou ao modelo seguinte, sem
parar e **sem pedir para o subir**, como mandaste.

---

## BLOCO D — fecho

### D1 e D2 — o que ficou desligado, de propósito

- **O ciclo automático de ordens (o "clique nº 8") continua desligado.** Não foi
  arrancado, por decisão tua, até o Guarda FC estar fechado.
- **O tecto de €1 do Gemini está como estava.** Bateu duas vezes esta sessão (uma no
  radar, uma no publicador) e nas duas o recurso funcionou sem eu pedir para o subir.
- **`radar-ia.sh`, loop e carteiro intactos.** O `radar-ia.sh` está provado pelo md5.

### Portão de RAM

| Momento | Medido | Decisão |
|---|---|---|
| Início da sessão | **504 MB** | acima do portão leve (400), mas o `llama-server` estava parado com 4256 MB |
| Depois de o matar | **4659 MB** | acima dos dois portões, leve e pesado |
| Antes do bloco B4 | **1305 MB** | acima dos dois; nada precisou de ser libertado |

O `playwright` e o `nano-banana` não estavam a correr, logo não havia nada a libertar
aí.

### Ordem dos blocos — uma coisa que declaro

A ordem dizia A → B → C → D. **Cumpri A por inteiro antes de começar o resto.** Mas
depois disso corri **B4 e C ao mesmo tempo**, em vez de um a seguir ao outro, porque
nada em C depende de B e os dois vivem na VPS. Para não se atropelarem, proibi os
dois de mexer no crontab e fui eu a acrescentar as duas linhas no fim, com a
diferença conferida antes e depois. Digo isto porque é um desvio à letra da ordem,
mesmo servindo a intenção dela.

### O que fica POR CONFIRMAR

1. **A página do Facebook e o Instagram não existem.** Travados no login. É o único
   clique que sobra.
2. **O caminho de sucesso do Gemini ficou por ver.** Só está provado o recurso (fundo
   liso da marca). O Gemini nunca chegou a devolver uma imagem nesta sessão.
3. **A cópia `guarda-fc-v7.pages.dev` continua por confirmar** no iPhone. Não foi
   publicada nesta sessão — os passos de deploy ficaram saltados em todas as corridas.
4. **Duas linhas de cron ainda não correram sozinhas.** O radar de dinheiro só dispara
   na quarta e o publicador na segunda. Ambos foram forçados à mão e funcionaram; a
   corrida automática é que ainda não aconteceu.
5. **Os grupos de Facebook estão por levantar.** A tabela em `grupos.md` está vazia de
   propósito: sem sessão não se vê o número de membros nem a regra de publicação.
6. **Dois parceiros estão marcados `coming_soon`** na base (Mr Kebab e Sabores de Casa
   Açaí) e por isso ficam fora das publicações. Se já abriram, é uma bandeira a mudar.
7. **O pagamento do Google Cloud está recusado.** Não toquei. Ver o aviso no topo.
8. **O ficheiro `SISTEMA-REDONDO-2026-08-21.md` que a ordem mandava ler não existe.**
   Procurei em todo o disco. O que existe é a família `sistema-redondo-*-2026-08-01.md`
   e o `FECHO_SEMANAL_SISTEMA.estado.md`, que li em vez dele.
9. **Não há job de `sitemap`** no Guarda FC, ao contrário do que a ordem supunha. O
   `sitemap.xml` é gerado dentro do `tools/build.mjs` a cada build.

### D3 — commit e push

Repositório real conferido antes: `nilofulfarotuga-hue/bora-app-cloud`, ramo
**`autonomous-night-2026-04-29`** (a 12/08 era outro; hoje é este).

**Antes de empurrar, li os gatilhos.** O `build_android.yml` e o
`build_web_deploy.yml` correm em push para este ramo, mas ambos têm
`paths-ignore` com `**.md` e `.claude/**`. Commitei **só** ficheiros dentro de
`.claude/`, e verifiquei a lista antes de empurrar.

```
e53a7dc1..6c075b3e  autonomous-night-2026-04-29 -> autonomous-night-2026-04-29
```

**Prova de que não disparou nada:** 45 segundos depois do push, a lista de corridas
do repositório não tem uma única entrada com o SHA `6c075b3e`. As mais recentes são
todas de commits anteriores.

Havia trabalho de outra sessão por commitar na mesma pasta (`lib/main.dart`,
`analysis_options.yaml`, ecrãs de admin, imagens de golden). **Não lhe toquei:**
pus de lado com `git stash`, rebaseei o meu commit, empurrei, e devolvi. Conferi a
lista antes e depois com `diff` — igual, nada se perdeu.

Cópia no vault: `C:\Users\danil\Desktop\Bora\Projetos\RELATORIO_tudo-02-09_2026-09-02.md`

### D4 — fecho do contexto

```
ctx doctor : Version v1.0.169 — Server test PASS, FTS5/SQLite PASS,
             6 hooks PASS, storage OK. Um aviso: instalar o Bun daria
             3-5x de velocidade.
ctx stats  : 39,4 MB mantidos fora do contexto nesta conversa (99,8%),
             291 capturas.
```

### Higiene

O ficheiro temporário com a credencial do GitHub que usei para ler os logs foi
apagado com `shred`. Procurei padrões de segredo a sério nos ficheiros que commitei:
nenhum. A única linha que dispara um alerta ingénuo é a minha própria descrição do
filtro de segurança do log do publicador.

### ⚠️ Um build da Play está a sair agora, e NÃO é meu

Ao confirmar que os meus commits não disparavam nada, apanhei isto:

```
Build Android & Deploy to Google Play  ce1103c5  09-02T22:48:51  in_progress
Build & Deploy Web (Cloudflare Pages)  ce1103c5  09-02T22:48:51  in_progress
olho-golden                            ce1103c5  09-02T22:48:51  success
```

O commit `ce1103c5` é de **outra sessão** que estava a trabalhar no mesmo ramo ao
mesmo tempo (mexeu em `lib/main.dart` e nos ecrãs de admin do WhatsApp). Empurrou
por volta das 22:48 e disparou build de Android e deploy web.

Digo-te porque a regra é "push é publicação" e tu tens de saber o que viaja. **Não
fui eu, e não interferi.** Os meus dois commits (`6c075b3e` e `cf466ca6`) só levam
ficheiros em `.claude/`, e a lista de corridas prova que nenhum deles disparou coisa
nenhuma.
