# MEGA-FIX 2026-07-18 — Relatório

Branch: `autonomous-night-2026-04-29` · Modo: PROTECÇÃO TOTAL · CEO-AI carregado.
Perímetro protegido respeitado (§1.6 Lista Vermelha): pricing_service, dispatch_engine,
finalizePurchase, bora_tokens, webhook Stripe, RLS de orders/wallets/ledger, tokens da Limpeza.

---

## PARTE 1 — Executor do loop: tarefa grande nunca mais morre muda

**Estado: JÁ APLICADA (FASE 1.10, commits `572efb1` + `0447dab`).**

Verificação (2026-07-18):
- `bora-live-parser.ps1` linha 49 → emite `EXECUTOR-PAROU: subtype=… turns=… custo=…` no ramo
  `'result'` quando não há `.result` nem `.error`. Regra dura "nunca 0 bytes" presente.
- `run-claude-loop.cmd` linhas 56-57 → `--max-budget-usd 25` + `--max-turns 150` (subido de 10/40).
- `carteiro.sh` linhas 187-530 → deteta `^EXECUTOR-PAROU:`, grava a linha exata como nota e TRAVA
  já (sem queimar as 5 tentativas). Selftest `executor_parou_linha` presente (linha 386-387).

Nada a fazer. Salto para a Parte 2 conforme instrução do prompt.

---

## PARTE 2 — Religar motor de aprendizagem + gravar lições

**Estado: FEITA.**

### Diagnóstico (a premissa "motor parado" era parcialmente falsa — reportado com honestidade)
- **robot-b (minerador Gemini)**: **VIVO**. Cron `robot-b-hourly` (jobid 35) corre de hora a hora
  e **todas** as execuções recentes = `succeeded / 1 row` (última 2026-07-17 22:07). Não está
  parado. Produziu poucas sugestões desde 07-15 porque a novidade de padrões no chat é baixa, não
  por avaria. Também vivos: `robot-b-weekly-digest` (45), `analyze-conversations-weekly` (28),
  `auto-archive-old-suggestions` (29).
- **evolution-engine (meta-evolução de skills)**: **reativo POR DESENHO**, não avariado. Depois do
  loop autorreferencial de ordens `-evol` (ver `inbox/evolution-engine-religado-2026-07-13.md`), o
  `hermes-evolution-trigger.sh` foi tornado stub inerte de propósito. A análise real só corre numa
  sessão Claude Code de verdade — como esta.
- Conclusão: "religar" = correr a análise agora nesta sessão (feito) + drenar o backlog (feito).

### Ações
1. Corri `evolution_engine.py --data 2026-07-18` (real, não dry-run) → produziu
   `inbox/evolution-report-2026-07-18.md` (50 skills analisadas, 2 propostas novas, 26 suprimidas).
   O ciclo de aprendizagem está comprovadamente a correr de novo.
2. Escritas 7 lições em `wiki/licoes/` (formato das existentes) + indexadas no `wiki/licoes/README.md`:
   `licao-dual-owner-column`, `licao-rpc-composite-null-row`, `licao-exception-when-others-null`,
   `licao-notify-canal-errado`, `licao-heartbeat-fantasma`, `licao-parser-mudo`,
   `licao-autocomplete-teclado`.
3. Processadas as 4 `skill_suggestions` pending → todas **REJEITADAS** com motivo (eram ruído de
   mensagem única, `message_count=1`, `suggested_skill_name=null`, acumuladas 06-26..07-15). Duas
   ("quero fala c humanos", "ninguém entrou em contacto") sinalizam necessidade real de handoff
   humano no support-chatbot — anotado no motivo, não vira skill nova.

### Ficheiros tocados
- `.claude/.ai/knowledge/wiki/licoes/licao-*.md` (7 novos) + `wiki/licoes/README.md` (índice)
- `.claude/.ai/knowledge/inbox/evolution-report-2026-07-18.md` (gerado pelo motor)
  + `.claude/skills/evolution-engine/scripts/state/propostas.json` (estado do motor)
- DB (não-repo): 4 linhas `skill_suggestions` → status `rejected`.

**Commit:** `bbbdd28` `fix(parte-2): religar motor de aprendizagem + 7 licoes + triar 4 skill_suggestions`
**Push:** OK via HTTPS+wincredman (`0447dab..bbbdd28`). Nota infra: SSH falha nesta sessão (só
`id_ed25519_vps`); push feito por `git -c credential.helper=manager push https://…` — ver
memória `project_push_https_wincredman`.

---

## PARTE 3 — Cadastro de parceiro grava o dono nas DUAS colunas

**Estado: FEITA.**

Prova prévia: `restaurants` tem `user_` (uuid) e `user_id` (uuid), ambas nullable. Insert do
`register-partner` gravava só `user_id`; RPCs de parceiro leem `user_`.

1. `register-partner/index.ts` — no **insert** e no **update** de `restaurants` grava agora
   `user_: userId` E `user_id: userId`. **Redeploy OK → versão 8** (verify_jwt=false preservado).
2. Migration `20260718000000_restaurants_mirror_owner_columns.sql` — trigger
   `trg_restaurants_mirror_owner` BEFORE INSERT OR UPDATE que espelha
   `user_ := COALESCE(user_, user_id)` e vice-versa (COALESCE nunca sobrescreve valor com NULL).
   **Aplicada via MCP** (`{"success":true}`). Verificação: 0 restaurantes com mismatch
   (`(user_ IS NULL) <> (user_id IS NULL)`), 5 donos consistentes de 20 (os outros 15 são
   mercados não-parceiro sem dono, correto). Backfill dos 5 já tinha sido feito (não repetido).
3. `restaurant_store.dart` `registerPartnerRestaurant()` (~L624) — código morto (0 chamadores no
   `lib/`, gravava só `user_`, id `partner-<micros>` legado) → marcado `@Deprecated` apontando
   para a Edge Fn. `flutter analyze lib/stores/restaurant_store.dart` → **No issues found**.

### Ficheiros tocados
- `supabase/functions/register-partner/index.ts` (+ redeploy v8)
- `supabase/migrations/20260718000000_restaurants_mirror_owner_columns.sql` (novo, aplicado)
- `lib/stores/restaurant_store.dart`

**Commit:** `103e056` · **Push:** OK (`bbbdd28..103e056`).

---

## PARTE 4 — Admin deixa de ser cego para serviços (beleza)

**Estado: FEITA.**

Descoberta que simplificou tudo: já existia `notify_admin_urgent_push()` (linha HIGH in-app +
FCM best-effort via `notify-admin-urgent`) e o **Gatilho 3** (2026-07-17) já notificava admins de
`restaurants` pending. Faltava só o equivalente para `service_providers`. Prova: candidatura
beleza "teste" (id `7d7f9c15…`, pending 17/07) estava invisível.

1. Migration `20260718001000_admin_visibility_service_providers.sql` (aplicada, `{"success":true}`):
   - `admin_list_partners_detailed` e `list_partners_by_status` → **UNION** `restaurants` +
     `service_providers` com coluna nova `source` ('restaurant' | 'service_provider'). (RETURNS
     TABLE mudou → DROP+CREATE.) Para SP o email/owner_name vêm de `auth.users` via `user_id`.
   - `approve_service_provider(text)` + `reject_service_provider(text,text)` — gémeas das de
     restaurant (auditoria `service_provider_approve/reject`), SEM tocar `approve_partner`/
     `reject_partner` (o Flutter escolhe pela `source`). GRANT a `authenticated`.
   - Gatilho `trg_admin_notif_service_provider_app` AFTER INSERT/UPDATE OF approval_status em
     `service_providers` → `notify_admin_urgent_push('service_provider_application_pending', …)`.
     (Reusa a plumbing existente — o prompt pedia `_push_in_app_notification`; usei o helper
     superior que faz in-app HIGH + FCM, que é exatamente o pedido "in-app + FCM se o canal existir".)
2. `admin_partners_pending_screen.dart`: badge de tipo (🍽️ Restaurante / 💇 Serviço) por `source`;
   aprovar/rejeitar roteiam para a RPC certa por `source` (`p_provider_id` vs `p_restaurant_id`). PT-BR.
3. `pending_approval_screen.dart`: texto "sob análise" agora reflete a categoria — "O teu salão"
   (beauty), "A tua loja" (store), "A tua farmácia", "O teu supermercado", "O teu restaurante",
   fallback "O teu negócio". `register_partner_screen.dart` passa `_selectedCategory.name`. PT-PT.

`flutter analyze` dos 4 Dart tocados: **0 erros, 0 issues novos** (as 8 warnings/info são
pré-existentes: helpers `_Partner*`, imports não usados, `value` deprecated — não introduzidas aqui).

### Ficheiros tocados
- `supabase/migrations/20260718001000_admin_visibility_service_providers.sql` (novo, aplicado)
- `lib/screens/admin/admin_partners_pending_screen.dart`
- `lib/screens/pending_approval_screen.dart`
- `lib/screens/register_partner_screen.dart` (só a navegação; toggles ficam na Parte 5)

**Commit:** `7a90388` · **Push:** OK (`103e056..7a90388`).

---

## PARTE 5 — Cartões do parceiro + toggles no registo + categoria no admin

**Estado: FEITA.**

1. **Cartões do parceiro** — `restaurant_options_screen.dart` **já estava feito** (BUG 2, 2026-07-17):
   os 3 cartões aparecem SEMPRE; "Ir buscar"/"Reservar mesa" usam `_buildOptionCard` com
   `Opacity 0.4 + IgnorePointer + "Não disponível"` quando `takeawayEnabled`/`reservationsEnabled`
   são false. Não-parceiro intocado. Nada a alterar — confirmado por leitura.
2. **Toggles no registo** — `register_partner_screen.dart`: 2 `SwitchListTile` ("Aceito reservas de
   mesa" / "Aceito ir buscar (takeaway)") junto ao checkbox de termos, **só quando categoria =
   restaurante**. Threading completo: `_acceptReservations`/`_acceptTakeaway` →
   `registerPartnerWithDocumentsAsync`/`resumePartnerRegistrationAsync` →
   `_submitRestaurantEdgeFunction` (body) → Edge Fn grava `reservations_enabled`/`takeaway_enabled`
   no `restaurantPayload` (insert E update). **register-partner redeploy v9** (verify_jwt=false).
   (`auth_store.dart` trazia também um fix pré-existente relacionado — `await _signInBackground`
   BUG 3 2026-07-17, para `auth.uid()` estar pronto antes dos toggles baterem na RLS — incluído.)
3. **Categoria no admin** — migration `20260718002000_admin_update_product_category.sql` (aplicada):
   RPC `admin_update_product_category(text,text)` (espelha `admin_update_product_price`: guard
   `_admin_op_guard` + audit, sem exigir motivo pois categoria não é dinheiro).
   `admin_partner_detail_screen.dart`: `_editCategory` (diálogo padrão do `_editPrice`) + ícone de
   edição no subtítulo "Categoria:". PT-BR.

`flutter analyze` dos 4 Dart tocados: **0 erros, 0 issues novos** (19 warnings/info são todos
pré-existentes: imports não usados, `use_build_context_synchronously`, `activeColor`/`value`
deprecated — não introduzidos aqui).

### Ficheiros tocados
- `lib/screens/register_partner_screen.dart`, `lib/auth/auth_store.dart`
- `supabase/functions/register-partner/index.ts` (+ redeploy v9)
- `supabase/migrations/20260718002000_admin_update_product_category.sql` (novo, aplicado)
- `lib/screens/admin/admin_partner_detail_screen.dart`

**Commit:** `52168ec` · **Push:** OK (`7a90388..52168ec`).

---

## PARTE 6 — Sobreposição + notificação persistente unificada

**Estado: FEITA (parcial deliberada — o núcleo reutilizável entregue; rewire dos fluxos que já
funcionam ADIADO para QA com device, por segurança).**

### O que JÁ existe (verificado por leitura — não reinventado)
- **Background (todos os papéis)**: o bg FCM handler em `notification_service.dart` já dispara
  notificação fullScreenIntent + som `bora_alert` para `new_order` (parceiro, L186), 
  `new_tvde_ride_offer` (TVDE, L207-266) e `new_order_offer` (estafeta, via `offer_presentation_gate`).
- **Foreground (todos os papéis)**: estafeta via `driver_home_screen._handleNewOrders`; **TVDE já
  apresenta a oferta em foreground** via `store.offeredRide → TvdeOfferScreen`
  (`tvde_driver_home_screen.dart` L240-246) + poll de 10s de backup; parceiro via realtime do dashboard.

### O que FIZ (aditivo, 0 risco de regressão, analyze limpo)
- **`lib/services/incoming_job_alert.dart` (novo)** — o padrão persistente do estafeta extraído
  num serviço reutilizável: `IncomingJobAlert.show({id,type,title,body})` / `dismiss(id)` no MESMO
  canal provado `bora_orders_urgent_v3` (fullScreenIntent + som em loop FLAG_INSISTENT + BigText).
  CallKit/CallStyle/OverlayWindow continuam PROIBIDOS. `flutter analyze` → **No issues found**.
  → A **profissional de limpeza** (que hoje não recebe NADA) passa a ter alerta insistente: o
  consumo deste serviço é ligado na **Parte 7** (onde também se corrige o canal/entrega).

### O que NÃO fiz e PORQUÊ (bloqueio honesto)
Rewire dos fluxos **estafeta/parceiro/TVDE** que JÁ apresentam oferta em foreground para passarem
pelo serviço novo, e a migração dos 6 blocos duplicados do bg handler, foram **deliberadamente
adiados**: é um refactor de um sistema de notificação delicado, testado em device e a FUNCIONAR
(som, heads-up, full-screen, insistent) que **não é verificável sem device físico Android** nesta
sessão. Mexer às cegas arriscava regredir a oferta do estafeta (feature-core) — CLAUDE.md "NEVER
break existing working features". Pontos de integração ficam prontos (o serviço existe); a
migração é 1 sessão de QA com telemóvel. O item 4 (TVDE foreground) já está coberto pelo
`offeredRide → TvdeOfferScreen` existente; o item 2 (tocar abre o card do parceiro) precisa do
router de tap + scroll-to-card, também melhor validado em device.

### Ficheiros tocados
- `lib/services/incoming_job_alert.dart` (novo)

---
