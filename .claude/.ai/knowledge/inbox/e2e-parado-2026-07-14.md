---
id: e2e-parado-2026-07-14
tipo: relatorio
origem: [executor autonomo — pedido direto do Danilo]
ultima_confirmacao: 2026-07-14
zona: verde
confianca: auto
---

# E2E automático parado a pedido do Danilo (2026-07-14)

Decisão do Danilo: vai testar manualmente, não quer mais o loop E2E automático a
correr sozinho. Ações tomadas, todas reversíveis.

## 1. Ordens pendentes / auto-encadeamento (c6e1 e seguintes)

Consultei a tabela `orders` diretamente (service role, read-only) por 3 vias:
- `customer_name ilike *TESTE E2E*` com estado não-terminal → **0 resultados**
- `assigned_driver_id` = estafeta de teste com estado não-terminal → **0 resultados**
- `id ilike c6e1*` → **0 resultados**
- Todos os pedidos não-terminais criados hoje (desde 00:00) → **0 resultados**

Não há nenhum pedido E2E pendente ou em progresso agora. Isto bate certo com o
último relatório do loop (`e2e-resultados-2026-07-14.md`, ciclo das 09:26): os 4
fluxos de encomenda (mercado/restaurante/etc.) ficaram **PENDENTE-HUMANO** porque
o telemóvel caiu do adb antes de qualquer pedido ser criado — não chegou a existir
"c6e1" nem categoria seguinte para arquivar. Se havia um pedido com esse prefixo
de uma corrida anterior, já não está em estado pendente na BD agora.

## 2. Mecanismo de auto-encadeamento — DESATIVADO

Achado: a tarefa agendada do Windows **`BoraE2E_LoopNoturno`** corria
`schtask-loop.cmd → loop-noturno.py` **de hora em hora** (repeat=PT1H), e era
essa tarefa que reiniciava o ciclo por todas as categorias do
`flows/registry.json` (smoke-login, delivery-mercado-cash,
delivery-restaurante-cash, tvde, etc.) sempre que havia fluxos ainda não
"verde-estáveis". Próxima corrida estava marcada para 09:38 (dentro de minutos).

Ações:
- `Disable-ScheduledTask -TaskName BoraE2E_LoopNoturno` → confirmado **State:
  Disabled**. Não apaguei a tarefa (reversível com `Enable-ScheduledTask` quando
  o Danilo quiser voltar a testar automático).
- Criado o ficheiro `.claude/testes-e2e/PARAR` — trava adicional que já é
  respeitada por `loop-noturno.py`, `espera-e-corre.cmd` e `watcher-loop.cmd`
  (todos verificam este ficheiro a cada ciclo/iteração antes de continuar).
- **Código dos testes NÃO foi tocado** — só o gatilho automático. O `runner.py`,
  `registry.json` e os YAMLs continuam intactos para corridas manuais futuras.

Outras tarefas agendadas relacionadas, verificadas e deixadas como estavam
(não fazem parte do auto-encadeamento de pedidos):
- `BoraE2E_MonitorProva` — gatilho único já passado (2026-07-11), sem próxima
  corrida agendada; inerte por si só.
- `BoraTesteFechadoMonitor` — monitor diário do Play Console (teste fechado),
  assunto diferente, não mexe em pedidos/BD de encomendas.

## 3. Processos ativos agora — nenhum a correr ou reconectar testes

Verificado via `Get-CimInstance Win32_Process` (todos os `python.exe`,
`java.exe`, `cmd.exe`, `powershell.exe` com a respetiva `CommandLine`):
- **Nenhum** processo `loop-noturno.py`, `runner.py`, `schtask-loop.cmd`,
  `watcher-loop.cmd` ou `espera-e-corre.cmd` em execução.
- `.claude/testes-e2e/.loop-noturno.lock` não existe → nenhuma instância viva.
- O processo Java `maestro.cli.AppKt mcp` presente é o **servidor MCP do
  Maestro** (ferramenta on-demand, fica sempre de pé para uso interativo) — não
  está a correr nenhum teste nem a reconectar ao device sozinho.
- 3 janelas `monitor-bora.cmd` abertas são só monitor visual (scrcpy +
  tail de log) — não disparam nem criam pedidos, deixei-as (o Danilo pode
  querer olhar para o ecrã do telemóvel enquanto testa à mão).

## Resumo

| Item | Estado |
|---|---|
| Pedidos E2E pendentes/em auto-encadeamento | Nenhum encontrado na BD |
| Tarefa agendada horária (`BoraE2E_LoopNoturno`) | **Desativada** (reversível) |
| Ficheiro `PARAR` | Criado |
| Processo de teste a correr agora | Nenhum |
| Código dos testes (`runner.py`, `registry.json`, YAMLs) | Intacto, não tocado |

**E2E AUTOMÁTICO PARADO — nada mais vai tentar correr.**
