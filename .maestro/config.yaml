# Maestro workspace config (.maestro/config.yaml).
# Glob patterns relative to this file's directory.
# Excludes _shared/ (sub-flows reusable via runFlow — não são entry points).

flows:
  - flows/cliente/*.yaml
  - flows/parceiro/*.yaml
  - flows/estafeta/*.yaml
  - flows/admin/*.yaml
