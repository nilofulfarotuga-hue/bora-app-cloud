---
tema: aprovador-vermelho-historico-corridas · escopo: agente:aprovador-vermelho · estado: atual · atualizado: 2026-07-27
---

# Aprovador-Vermelho — histórico de corridas (log append-only)

> Log detalhado, corrida a corrida, das triagens da fila `robot_suggestions`. Partido de
> `aprovador-vermelho-triagem.md` em 2026-07-20 pelo `bibliotecario-cerebro` (checagem #8 do
> protocolo — o ficheiro principal tinha passado de ~24 KB). O conhecimento durável (regras
> Balde A/B, funções cron conhecidas, anomalias, mecanismos de aprovação) fica no ficheiro
> principal; aqui só o log cronológico de corridas.
>
> **Corridas 2026-07-10 a 2026-07-16** (todos os lotes já fechados/decididos pelo Danilo) foram
> movidas em 2026-07-21 (checagem #8 — este ficheiro tinha chegado a 24123 bytes) para
> `aprovador-vermelho-historico-corridas-arquivo-2026-07-ate-16.md`. **Corridas 2026-07-18 e
> 2026-07-19** (as mais extensas do log) foram movidas em 2026-07-24 (checagem #8 — este ficheiro
> tinha chegado a 23774 bytes) para
> `aprovador-vermelho-historico-corridas-arquivo-2026-07-18-a-19.md`. **Corridas 2026-07-20 e
> 2026-07-21** (múltiplas reconfirmações do mesmo item no mesmo dia, item principal de ambas já
> decidido pelo Danilo) foram movidas em 2026-07-27 (checagem #8 — este ficheiro tinha chegado a
> 25041 bytes, acima do limite de ~24 KB) para
> `aprovador-vermelho-historico-corridas-arquivo-2026-07-20-a-21.md`. Este ficheiro fica só com as
> corridas ativas/recentes (2026-07-24 em diante).

## Histórico de corridas
| Data | Tipo | Resultado |
|---|---|---|
| 2026-07-24 (corrida FALLBACK_30MIN, gatilho: item mais antigo em `nova` parado ~3192min/~53h) | 4 itens em `nova`, estado real confirmado via `execute_sql` direto (não assumido de memória) | **0 Balde A.** **4 Balde B — todos família nova `marcacoes:*`** (ver secção dedicada em `aprovador-vermelho-triagem.md`): `1efa3e60-10de-423c-97fb-8a21148de370` (dedup_key `marcacoes:liberar-slots-orfãos-ttl`, criado 2026-07-22T02:07:13, reconfirmado) e `47a4a9e6-07c7-4846-864a-e400064c9b0a` (dedup_key `marcacoes:ttl-pending-payment`, criado 2026-07-22T06:07:13, reconfirmado, praticamente duplicado do anterior) — **prova nova nesta corrida:** a reserva órfã que sustentava ambos (`7c61663d-ec39-48a2-83b4-5e4cc081794f`, `prepayment_pi pi_3TvmY8GlT3R2jCYp1thQswqy`, €3) já **não existe** em `reservations` (0 linhas, SELECT direto) e hoje **zero** reservas estão `status='pending_payment'` — incidente resolvido sozinho (provável `cancel_orphan_reservation()` via webhook Stripe, não confirmado qual); não promove os itens a Balde A, a proposta em si continua a automatizar escrita/cancelamento de reservas pagas sem regra de refund definida. `c068f901-e877-4df0-8b43-0ac1b1c04234` (dedup_key `marcacoes:ajustar-politica-no-show`, criado 2026-07-24T04:07:14, **1ª ocorrência**, propõe `deposit_required_threshold:0.5` — crons de lembrete já ativos, só a parte do depósito é novidade real) e `7cf1a393-82b5-40d6-8738-7d300e73f85a` (dedup_key `marcacoes:resolver-marcacoes-orfas`, criado 2026-07-24T05:07:16, **1ª ocorrência**, `nivel=3`, `payload_execucao=null`, evidência com 2 reservas já stale) — ambos novos, primeira triagem, Balde B. Funções confirmadas em produção nesta corrida (`pg_get_functiondef` + leitura de `supabase/migrations/`): `cancel_orphan_reservation()` (`20260515100002_cancel_orphan_reservation.sql`) e `auto_close_no_show_reservations()` (`20260503020000_reservation_rpcs_sync.sql:119`, cron correspondente `active=false`). Nenhuma das 3 chaves propostas existe hoje em `platform_settings` nem é consumida por função/cron algum (confirmado por grep local + SELECT). Telegram consolidado enviado com sucesso (bridge SSH PC→VPS, `id_ed25519_vps`, `Sent to telegram home channel`, chat_id `6731890157`, exit 0) — 1 mensagem cobrindo os 4 itens. `admin_audit_log`: 4 linhas novas (`robot_suggestion_baldeB_reconfirmado` ×2, `robot_suggestion_baldeB_surfaced` ×2), `created_at` ~2026-07-24 07:2x UTC. `aprovador_vermelho_auto_baldeA=true` reconfirmado, sem efeito (0 Balde A). Nenhuma escrita em `reservations`/`platform_settings` financeiros/zona protegida — só roteamento (`admin_audit_log`) + aviso Telegram. Relatório: `.claude/.ai/reports/aprovador-vermelho-2026-07-24-fallback30min-1.md`. **2ª corrida (FALLBACK_30MIN, ~07:39 UTC, gap ~12min desde a 1ª):** mesma fila, byte-a-byte — os 4 IDs/`dedup_key`/`created_at`/`status='nova'` idênticos à 1ª corrida (confirmado por SELECT direto), zero evidência nova. Telegram **suprimido** (protocolo anti-spam: último envio real 07:27:50 UTC, gap ~12min, abaixo do limiar ~60min). `admin_audit_log`: 4 linhas novas (`robot_suggestion_baldeB_reconfirmado` ×4, `telegram_enviado=false`, `motivo_supressao_telegram` explícito). 0 Balde A, nenhuma auto-aprovação, nenhuma escrita em zona protegida. **Sinal observado (a confirmar, amostra n=2 corridas):** o FALLBACK_30MIN disparou 2× em ~12min para o mesmo watermark/item mais antigo — possível reincidência da anomalia já conhecida de gatilho/cooldown de `STALE_MIN` partilhado (ver "Anomalia conhecida" em `aprovador-vermelho-triagem.md`, já reconfirmada em 2026-07-20 com gaps de 5-8min entre corridas), ou apenas coincidência de agendamento do cron VPS; fora da fronteira de roteamento do `aprovador-vermelho` — registado para quem cuidar do loop/gatilhos (`maestro-autonomia` ou revisão de `hermes-aprovador-vermelho.sh`) avaliar. Relatório: `.claude/.ai/reports/aprovador-vermelho-2026-07-24-fallback30min-2.md`. |
| 2026-07-27 (corrida FALLBACK_30MIN, gatilho: item mais antigo em `nova` parado 8002+min) | 30 itens em `nova` (histórico normal: 1-9) — MCP Supabase indisponível nesta sessão (`.mcp.json` só listava `nano-banana`/`graphify`); consulta via PostgREST direto (`SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` de `backend/.env`, mesmo efeito de `execute_sql`, bypassa RLS) | **2 Balde A auto-aprovados** (`platform_settings.aprovador_vermelho_auto_baldeA=true` confirmado antes): `9391dfac-00ac-4eaf-b5d9-1e1732bf1dd9` (dedup_key `infra:otimizar-cron-jobs-marcacoes-drivers`, categoria rotulada `marcacoes` pelo gerador mas conteúdo real = só `_cron_check_orphan_orders()` + `_cron_check_ghost_drivers()`, SEM `_appointment_cron_auto_no_show` — ambas já Balde A confirmadas; reconfirmado por leitura de `supabase/migrations/20260518112559_admin_notif_crons_4_6_7.sql`; `admin_audit_log` id `af058569-80c9-4ba8-88ce-13baa141cadf`) e `a950e75e-c03a-4682-8762-0e20ab414c5d` (dedup_key `catalogo:backlog-produtos-revisao`, nível 3, evidência `count=3743` — mesmo padrão do precedente `6a38f26a-a9e0-4d54-bc72-16db02430e91` de 2026-07-16, plano de revisão sem escrita direta; `admin_audit_log` id `cab62b87-c73d-4028-a044-85ab06b70763`). **28 Balde B ficam `status='nova'`:** família conhecida `marcacoes:*` reconfirmada (`1efa3e60` liberar-slots-órfãos-ttl, `c068f901` ajustar-política-no-show com `deposit_required_threshold:0.5`, `7cf1a393` resolver-marcações-órfãs nível 3) + `infra_cron` agrupado `13ec022c` (dedup_key `infra:otimizar-queries-lentas-cron`, inclui `_appointment_cron_auto_no_show` junto às 2 funções Balde A → item inteiro Balde B, regra de item agrupado) + **21 itens do achado novo** (17× `marcacoes:ajustar-no-show-rate-threshold[-v2..-v17]` + 4× `marcacoes:resolver-marcacoes-pendentes-orfas[-v2/-v3]`/`2efe0a26901c`) + **3 itens do subpadrão novo `catalogo:*ocultar`** (`3a6d6cd0`+`048e7340`-v2 "ocultar produtos sem foto" 59 produtos, `222e0b2b` "ocultar produtos sem categoria" 1546 produtos — `UPDATE is_available=false` em massa proposto diretamente, distinto do precedente Balde A "marcar para revisão" sem escrita direta; Balde B por dúvida/precedente, não por ser dinheiro). **ACHADO NOVO — bug do gerador de sugestões (dedupe quebrado), motivo real do salto 1-9→30:** 17 variantes de "Ajustar o no-show rate" com evidência IDÊNTICA (`{"total":3,"no_show":1}`) criadas a cada ~1-3h entre 2026-07-25 22h e 2026-07-27 05h, propondo a setting `reservation_no_show_rate_threshold` que **não existe** (distinta da já existente `reservation_no_show_threshold_count=3`, confirmado por grep local); + 4 variantes de "Resolver marcações pendentes órfãs" citando os mesmos 2 IDs de reserva (`7c61663d-ec39-48a2-83b4-5e4cc081794f`, `091ac601-4f54-4107-ab66-4ff6b7f2e55c`) — reconfirmado por SELECT direto: **0 linhas em `reservations` para ambos** (mesmo achado já registado em 2026-07-24 para o 1º ID; gerador continua a reciclar evidência fantasma). Bug de ROBUSTEZ do pipeline gerador (evolution-engine/robot-b), fora do mandato do `aprovador-vermelho` corrigir; candidato a ordem para `maestro-autonomia`. Ver detalhe completo em `aprovador-vermelho-triagem.md` (secção "Achado 2026-07-27") e `permanente/procedural/licoes/licao-spam-ordens-autoreferencial.md` (manifestação relacionada). Telegram enviado com sucesso (bridge SSH PC→VPS, `Sent to telegram home channel`, chat_id `6731890157`, exit 0) — gap ~3 dias desde o último envio real (2026-07-24 08:13:43 UTC), mensagem consolidada única cobrindo os 2 Balde A + resumo dos 28 Balde B. `admin_audit_log`: 30 linhas novas (`robot_suggestion_approved_baldeA` ×2, `robot_suggestion_baldeB_reconfirmado` ×3, `robot_suggestion_baldeB_surfaced` ×25). Nenhuma edição de código/migration/RPC/trigger/`platform_settings` financeiro, nenhuma escrita em `reservations`/`orders`/`wallets`/`ledger`/`bora_tokens` — só roteamento. Relatório: `.claude/.ai/reports/aprovador-vermelho-2026-07-27-fallback30min.md`. **2ª corrida (~17:22 UTC, gap ~1h43min desde a 1ª; gatilho: executor headless reportou fila `nova`=30, newest=2026-07-27T17:07:16 UTC):** comparação item-a-item confirma **28 dos 30 itens já tinham sido triados pela 1ª corrida** (`reviewed_at≈15:43 UTC`, mesmos IDs/`dedup_key`/evidência) — nenhum re-trabalho, nenhuma reconfirmação de auditoria/Telegram para eles (evita spam). **Só 2 itens genuinamente novos** (criados entre 16:07 e 17:07 UTC): **1 Balde A auto-aprovado** — `a68ff603-d5c4-48b8-8755-47a91f5f0e78` (dedup_key `catalogo:produtos-preco-suspeito-revisao`, 7 produtos, `payload_execucao={"type":"flag_products_review",...}` — só marca flag, zero escrita em preço; estende a família Balde A "marcar para revisão" de 2026-07-19 à variante "preço suspeito"; `admin_audit_log` id `1bfb8be9-94de-4dcc-891b-779452cb643c`). **1 Balde B novo, encaminhado** — `b454e9bc-b87d-4451-b861-fc2596a6f652` (dedup_key `catalogo:produtos-sem-categoria-revisao-ocultar`, nível 3, severidade 4, evidência `count=1546` idêntica ao item irmão já Balde B `222e0b2b-e691-4074-a52a-f366a77df476` — funde os nomes dos 2 subpadrões `catalogo` antagônicos (revisão=Balde A vs ocultar=Balde B) num único dedup_key; classificado Balde B porque o texto propõe "marcar para revisão e, após análise, ocultá-los" e a evidência bate com o lote já Balde B; `payload_execucao=null` não muda a classificação dado nível/severidade altos; `admin_audit_log` id `9c7cd788-e074-4a88-ab6e-92c0ac5f4f39`). Sinal adicional (junto ao achado de dedupe quebrado da 1ª corrida) de que o gerador de sugestões também mistura nomes de subpadrões antagônicos no mesmo `dedup_key` — mesmo bug de robustez do pipeline, não nova categoria de dinheiro. Telegram enviado com sucesso (bridge SSH PC→VPS, `Sent to telegram home channel`, chat_id `6731890157`, exit 0) — 1 mensagem cobrindo só os 2 itens novos, sem reavisar os 28 já surfaçados. Relatório: `.claude/.ai/reports/aprovador-vermelho-2026-07-27-1707z.md`. **3ª corrida (FALLBACK_30MIN):** fila 100% idêntica à da 2ª corrida — **0 itens novos**; os mesmos 29 IDs/`dedup_key`/`created_at`/evidência já triados nas corridas anteriores de hoje (confirmado por SELECT direto, `robot_suggestions.status='nova'` count=29 reconfirmado nesta consolidação). **0 Balde A** (os 3 já vistos hoje — `9391dfac`, `a950e75e`, `a68ff603` — já saíram de `nova` em corridas anteriores). **29 Balde B**, todos reconfirmados via `admin_audit_log` (ação `robot_suggestion_baldeB_reconfirmado`, 29 linhas, `telegram_enviado:false`) — **timestamp real confirmado por SELECT direto nesta consolidação: `created_at` 2026-07-27T17:40:33.097901 UTC**, não ~17:29 UTC como o relatório original estimara. Telegram suprimido — **gap real ~18min23s** desde o `baldeB_surfaced` anterior de 17:22:09.946 UTC (corrigido pelo bibliotecário: o relatório original tinha estimado ~7min; a evidência de `admin_audit_log` não bate com essa estimativa, mas a conclusão qualitativa fica — 18min continua abaixo do limiar anti-spam ~60min). Nenhuma mudança de `status` em `robot_suggestions` (todos continuam `nova`). **Sinal novo confirmado:** `platform_settings.robot_b_max_open_suggestions=30` (SELECT direto) e a fila em 29 — a 1 item do teto; combinado com o bug de dedupe do gerador já conhecido (famílias `marcacoes:ajustar-no-show-rate-threshold[-v2..-v17]` e `marcacoes:resolver-marcacoes-*-orfas`, ver `aprovador-vermelho-triagem.md` secção "Achado 2026-07-27"), o próximo disparo do gerador pode colidir com o teto — não urgente, nada quebrou, sinal a acompanhar. Relatório: `.claude/.ai/reports/aprovador-vermelho-2026-07-27-fallback30min-29itens.md`. **4ª corrida
(~17:51:43 UTC, gap ~11min desde a 3ª):** mesmos 29 IDs, mesma classificação (0 Balde A · 29 Balde
B), nenhuma escrita nova. Telegram suprimido (gap abaixo do limiar ~60min). `admin_audit_log`: 29
linhas `robot_suggestion_baldeB_reconfirmado`. Relatório:
`.claude/.ai/reports/aprovador-vermelho-2026-07-27-1750z-4a-corrida.md`. **5ª corrida (~18:02 UTC,
gap ~10min desde a 4ª):** mesmo lote idêntico, 0 Balde A · 29 Balde B,
`aprovador_vermelho_auto_baldeA=true` sem efeito (nenhum Balde A no lote). Mudança de prática: em
vez de repetir 29 linhas individuais em `admin_audit_log`, escreveu **1 linha consolidada**
(`robot_suggestion_baldeB_reconfirmado_lote`, `entity_id_text='lote_29_fallback30min'`,
`details.balde_b_ids` com os 29 IDs) para conter o ruído — a observar se essa prática se repete.
Telegram suprimido (gap ~10min). 5ª reconfirmação do mesmo lote em ~1h20 (gaps 25/18/11/10min)
confirma que o gatilho martela sem respeitar o backoff esperado (detalhe em
`aprovador-vermelho-anomalia-backoff-script.md`) — a fila em si seguiu corretamente classificada o
tempo todo. Relatório: `.claude/.ai/reports/aprovador-vermelho-2026-07-27-1802z-5a-corrida.md`. |

## Ver também
- `permanente/procedural/aprovador-vermelho-triagem.md` — conhecimento de triagem (regras Balde A/B, funções cron conhecidas, anomalias, mecanismos de aprovação/aviso).
- `permanente/procedural/aprovador-vermelho-anomalia-backoff-script.md` — anomalia do backoff exponencial em `hermes-aprovador-vermelho.sh` (partido de `aprovador-vermelho-triagem.md` em 2026-07-27).
- `permanente/procedural/aprovador-vermelho-historico-corridas-arquivo-2026-07-20-a-21.md` — corridas arquivadas (2026-07-20 a 2026-07-21).
- `permanente/procedural/aprovador-vermelho-historico-corridas-arquivo-2026-07-18-a-19.md` — corridas arquivadas (2026-07-18 a 2026-07-19).
- `permanente/procedural/aprovador-vermelho-historico-corridas-arquivo-2026-07-ate-16.md` — corridas arquivadas (2026-07-10 a 2026-07-16, todas fechadas/decididas).
