# FABLE — 13/09/2026 — Janela 1 — relatório da missão `fable-13-09`

> Motor: Claude Fable 5.1 (Claude Code, PC Windows Lenovo) · Modo protecção total · Início 13/09 ~17:30, fecho ~22:xx.
> Ramo de produção: `autonomous-night-2026-04-29` · Repo `nilofulfarotuga-hue/bora-app-cloud` · Supabase `ojykpzwqrtusfeakzrna`.
> Rasto: fluxo `fable-13-09` no `e2e_log` (linhas 1688 a 17xx) · provas em `.claude/.ai/provas/fable-13-09/`.

## 0. Acessos que usei (e os que não pude usar)

- Supabase por MCP (SQL, migrações, funções Edge, chaves públicas): usado para ler produção, aplicar as migrações e fazer as provas em rollback.
- Git por HTTPS com o gestor de credenciais do Windows: usado para trazer, unir e empurrar a produção.
- Cloudflare Pages por wrangler (o script `deploy-cloudflare.sh` do bora-site): usado para publicar o `/baixar` (deploy 43ab2a96).
- Emulador Android local (AVD `emdia`, Android 14 com Play Store) e `adb`: usado para o autoteste dos 3 perfis.
- Serviço público da Apple (`itunes.apple.com/lookup`) e o site `app.boraguarda.com`: usados para ler o estado da App Store e a chave publicável da Stripe (é pública).
- VPS Hostinger por SSH (a ponte do Telegram): usada só para a mensagem de fecho.
- Córtex por MCP: **não disponível** nesta sessão (pede autenticação OAuth). Nada foi memorizado no Córtex; a ordem de continuação ficou em ficheiro.
- Linha de comandos `gh` e API do GitHub: `gh` não existe neste PC. O verde do CI prova-se pelo valor `app_latest_version_code` em `platform_settings`, que o último passo do workflow só grava se o upload ao Play passou.

## 1. O que NÃO ficou feito, ou ficou "por confirmar" (lê isto primeiro)

1. **Bloco 5** acabou por caber (feito enquanto o APK compilava) — ver §3. A ordem de continuação `.claude/.ai/inbox/CONTINUAR-fable-13-09.md` fica só com os restos que precisam de ordem nova.
2. **4.2 autoteste local no emulador** — EM CURSO no fecho deste ficheiro (ver §3, Bloco 4.2 para o estado final). A primeira corrida falhou fora da app: o único Java do PC é o 25 do Android Studio e o Gradle 8.14 do projeto só aceita até 21; resolvido com um JDK 17 portátil (`C:\Users\danil\jdk17`) e `flutter config --jdk-dir`, sem tocar no Gradle do repo.
3. **4.3 push + CI verde + versionCode a subir** — depende do ponto 2 ("sem verde, nada é publicado"); estado final em §3, Bloco 4.3.
4. **Gravação de ecrã do talão (Bloco 2)** — não fiz filme. A prova do talão é um pedido real de teste em produção (36e6812a, dinheiro) fechado pela função nova e entregue, com SELECTs, mais o caminho cartão/MB Way provado em rollback. O arnês dos 3 perfis (4.2) tira capturas, mas não filma o talão.
5. **Espelho do `dispatch-engine` no repo** — produção corre a v61 (cabeçalho v59: a chave da oferta é `drivers.user_id`); o ficheiro do repo está na v58. A pasta é protegida pela Trava e não a copiei. Comportamento verificado e coerente com `driver_accept_offer` e com o Flutter (ver Bloco 1.4). Fica registado para uma janela com ordem explícita.
6. **A linha LOCAL do `ios-lancamento`** (52 commits: `orquestracao/agentes/*`, docs, cherry-picks com outro hash) **não entrou na produção**: unir com o `origin/ios-lancamento` dava 11 ficheiros em conflito. A produção nasceu do remoto (a linha que gerou a build 115 aprovada). O trabalho dos agentes continua no ramo local do PC.
7. **Córtex** — nada foi memorizado no Córtex (MCP sem autenticação). A memória desta janela ficou nos ficheiros de memória do Claude Code (5 notas) e no digest `digest-2026-09-13-janela-1` em `claude_ai_memoria`.
8. **Loja portuguesa da App Store** — continua fechada por acto de conta (estatuto de comerciante). Ver §5.

## 2. ⚠️ ISTO MEXE EM PAGAMENTO/DINHEIRO

A ordem desta missão mandava **aplicar e provar** (Blocos 1.3, 2 e 3: "zonas protegidas só com teste real"). Apliquei em produção, cada uma com prova em rollback com o JWT da pessoa real e/ou pedido de teste real. Lista fechada do que mexe em dinheiro:

- Migração `20260913193128_tvde_finish_respeita_credito_promocional`: o fecho da corrida TVDE passa a descontar o crédito promocional (o cliente paga tarifa menos crédito, o motorista ganha o normal, a Bora absorve). Qualquer cancelamento devolve o crédito. Prova na linha 1702 do e2e_log, em rollback com a cliente 93a6d3ff: corrida de 8 km fecha a 2,00 € para o cliente, 5,60 € para o motorista, menos 3,60 € para a Bora, crédito de 5,00 € marcado como usado; e devolvido depois de um cancelamento por falta de motorista.
- Migração `20260913194352_credito_promocional_fk_adiada`: a chave estrangeira do crédito passa a adiada. Era um bug real: a cliente com crédito não conseguia pedir corrida nenhuma. Prova na linha 1697.
- Migração `20260913193234_talao_nao_parceiro_uma_so_funcao`: uma só função fecha o talão do não-parceiro (talão, totais com o desvio financeiro autorizado, acerto do estafeta, estado a caminho), para todos os meios de pagamento; mais a função que regista erros de ciclo. Provas nas linhas 1704 e 1705 (pedido real 36e6812a, dinheiro) e 1717 (cartão/MB Way em rollback).
- Migração `20260913193545_plano_a_medida_vina_5_euros_ate_15_km`: tabela de preço fixo por cliente; as quatro funções de tarifa TVDE respeitam-na até ao limite de quilómetros; plano antigo da Vina desactivado; linha da Vina a 5,00 € até 15 km. Prova na linha 1701, em rollback com o JWT da Vina: 8 km dá 5,00 € ao cliente, 5,60 € ao motorista e menos 0,60 € à Bora; 16 km dá 15,00 € normal; outro cliente fica igual.
- Migração `20260913193756_admin_corrige_talao_com_auditoria`: função só para admin que corrige o valor do talão com motivo e deixa linha na auditoria. Ecrã admin ligado.
- Migração `20260913195205_pedido_demo_com_driver_id`: só contas demo; a caixa fechada passa a preencher o campo do estafeta que a guarda nova de 12/09 exigia. Provas nas linhas 1706 e 1707.

Nada em `platform_settings` financeiro foi alterado. Nenhum preço, taxa ou comissão geral mudou: a regra da Vina é só para o `user_id` dela. Se quiseres desfazer alguma, cada migração tem o SQL exacto em `supabase/migrations/` — dizes qual e eu reverto com a mesma prova.

## 3. Bloco a bloco (feito → prova)

### Bloco 0 — reconhecimento (e2e 1688, 1691)
- PC novo (`C:\BoraLocal`), ramo `ios-lancamento` 50 à frente / 9 atrás da produção; alterações alheias da sessão de 12/09 guardadas em stash nomeado `alheias-fable-13-09`; `index.lock` preso desde 12/09 22:20 movido para `C:\Users\danil\_QUARENTENA\2026-09-13\fable-13-09\` (índice CSV).
- 17 migrações de produção desde 09/09 fora do repo ou com timestamp/conteúdo errado; 5 espelhos errados (timestamps 20260912101500…160000) e um teste alheio (`test/tvde_chat_delivery_test.dart`) foram para a quarentena.

### Bloco 1 — auditoria das migrações do ChatGPT de 12/09
- **1.1 Espelhos** (e2e 1692, 1714): 17 migrações espelhadas com o timestamp de produção e SQL literal; md5 igual em 17/17 contra `schema_migrations.statements[1]`. Nas 5 do ChatGPT com CRLF, o git normaliza para LF (`core.autocrlf=true`) — o blob que o CI recebe tem md5 igual à produção normalizada (23/23 no total, contando as 6 novas). Nada foi reaplicado.
- **1.2 CHECK dos estados** (e2e 1693): `orders_status_canonical_check` = 10 estados. Varredura: `lib/` (enum + escritas), Edge (`stripe-webhook` e `finalize-order-from-intent` escrevem `callingDriver`; `dispatch-engine` lê), `pg_proc` — nenhum literal fora dos 10.
- **1.3 Crédito promocional** (e2e 1694, 1697, 1702): ver §2. Bug real apanhado pela prova: a FK impedia a cliente com crédito de pedir corrida.
- **1.4 `driver_accept_offer` / `driver_finish_cash_order`** (e2e 1705): pedido real 36e6812a → `finalizar_talao_nao_parceiro` → `onTheWay` → `driver_finish_cash_order` → `delivered`, `payment_status paid`. Gatilhos AFTER UPDATE dispararam dentro da SECURITY DEFINER: tokens cliente 97 / estafeta 40; `driver_transactions` ganho 5,69 € + acerto de dinheiro −3,18 €; ledger 5,69/3,18 + comissão da plataforma 0,99 €; `driver_balances` 0 → 8,87 €; 2 linhas em `order_status_events`; OCR correu; 0 linhas em `order_lifecycle_errors`. Dispatch v61 em produção (chave da oferta = `drivers.user_id`) coerente com a função e com o Flutter (`result['ok']`). Bug real (e2e 1706/1707): a guarda `order_driver_required` partia todos os pedidos das contas demo — corrigido.
- **1.5 Auto-dispatch de parceiro** (e2e 1698): chave `partner_auto_dispatch_after_minutes` já existia (categoria dispatch, valor 5); cron `partner-auto-dispatch` jobid 72 a cada 30 s; a função só apanha parceiro + `restaurant` + (dinheiro ou pago) + sem estafeta; `partner_mark_ready` idempotente (a Goola não é chamada duas vezes); erros vão a `order_lifecycle_errors`. Admin: chave editável no ecrã de definições (`admin_platform_settings_screen.dart`).
- **1.6 `order_status_events`** (e2e 1699): trigger `trg_record_order_status_event` existe e escreve; painel: Timeline do detalhe do pedido lê a tabela (`admin_order_detail_screen.dart`).
- **1.7 `match_cortex_knowledge`** (e2e 1700): lê `support_knowledge_chunks` (605 linhas). Só registado.
- **1.8** cada subitem tem a sua linha no `e2e_log`.

### Bloco 2 — talão não-parceiro que não fechava (e2e 1695, 1704)
- Causa: o Flutter fazia PATCH às colunas financeiras de `orders` a partir do estafeta, e o bloqueio financeiro (`orders_financial_lock`) recusava — o botão "Confirmar" morria sem erro visível.
- Feito: **uma só função** `finalizar_talao_nao_parceiro` (SECURITY DEFINER, todo o não-parceiro, todos os meios de pagamento): valida parceiro/estafeta/estado, guarda o talão em `order_receipts_v2`, itens (comprado/em falta/substituído/adicionado, com desconto em dinheiro ou crédito na carteira), sacos, totais com bypass, acerto do estafeta (`cash_settled` / `pending_admin`), chama OCR + aviso ao admin + aviso de compra finalizada, e passa a `onTheWay`. Idempotente.
- Flutter (`lib/services/talao_nao_parceiro_service.dart`, `driver_map_screen.dart`): deixa de tocar em colunas financeiras; erro real em PT-PT com código (diálogo com texto seleccionável) e registado em `order_lifecycle_errors` via `registar_erro_ciclo_pedido`; ao confirmar, navegação directa para a morada do cliente (`NavigationService.openTurnByTurn`). O cliente nunca vê o talão (a função não expõe nada ao cliente; o ecrã do cliente não lê `order_receipts_v2`).
- Admin (`admin_receipts_screen.dart`): ver e **corrigir o valor com motivo** → `admin_corrigir_talao` → `admin_audit_log`.
- Prova real: pedido 36e6812a (cópia do McDonald's, cliente de teste, estafeta demo, dinheiro, `is_test_order`) — resultado literal na linha 1704 do `e2e_log` (`ok:true, status:onTheWay, cash_total_due_cents:3241, reimbursement_status:cash_settled`) e depois entregue (1705).
- Prova cartão/MB Way (e2e 1717, rollback, nada ficou): cópia do 36e6812a com `mbway`/`paid` → `ok:true, status:onTheWay, reimbursement_status:pending_admin, final_purchase_value_cents:3241`; `orders.cash_total_due` fica NULL (não é dinheiro); recibo com `reimbursement_amount_cents 3241` à espera do admin; segunda chamada `idempotent:true, status_changed:false`; 0 erros de ciclo. Nota cosmética: na resposta idempotente `final_purchase_value_cents` vem 0 e `bag_count` null — o Flutter não os lê.

### Bloco 3 — Vina Cá (e2e 1696, 1701)
- Assinatura antiga `a20782f7` (plano especial, 8 corridas, 10 km, 4,50 € ao motorista) desactivada por completo (`active=false`, `ends_at=now()`).
- **Os +2 €**: o plano cobria 10 km e o pedido cobrava `CEIL(km − 10) × 1,00 €` por cima; um trajecto de 12 km mostrava +2 €. A Vina nunca chegou a ter corrida registada — o valor apareceu no orçamento.
- Regra nova só para o `user_id` dela: ≤ 15 km → 5,00 € fixo; > 15 km → tarifa normal. Motorista ganha o normal (a Bora absorve a diferença). Prova em rollback com os dois JWT (linha 1701).

### Bloco 4 — pós-Apple
- **4.1 Merge** (e2e 1709, 1711 e a correcção abaixo): produção local = merge `--no-ff` de `origin/ios-lancamento` (9bbd04ed; 2 conflitos resolvidos à mão: `lib/main.dart` união, `market_store_tab.dart` fica a produção) + cherry-picks 0f755f96 (espelhos), a07b6ef4 (correcções), 7e06a3da (docs + demo), 4c8e354b (workflow). Sem force em lado nenhum.
  **Erro meu apanhado antes do push:** a linha 1711 do e2e_log dizia "ff para origin/autonomous (5f183c6f)", mas o merge 9bbd04ed partiu de `d81cb040` (a produção local antiga): os 7 commits do remoto (PR #2 do Codex de 12/09 — `order_store.dart` com as transições atómicas, chat TVDE resistente ao realtime, testes, bump 603) tinham ficado **de fora**, e o `git push` seria rejeitado (não era fast-forward). Descobri ao medir "o que viaja" ficheiro a ficheiro: 5 ficheiros tinham regredido para a versão base. Corrigido com um merge de `origin/autonomous-night-2026-04-29` por cima (6 conflitos: as 4 migrações do Codex ficam na versão espelhada — md5 igual à produção, as do repo do Codex eram diferentes do que foi aplicado; `admin_order_detail_screen.dart` só formatação; `tvde_chat_screen.dart` fica com o bloqueio do iOS mais o `syncError` do Codex). Depois do merge, `origin/autonomous-night` é ancestral do HEAD e os 18 ficheiros que a produção tinha mudado ficam iguais à produção, excepto os que eu mudei de propósito. Antes do push voltei a buscar o remoto: tinham entrado mais 2 commits do espelho horário do Córtex (`[skip ci]`, só `.claude/`), unidos em ff596cf9; o `git push --dry-run` passou a aceitar (41f6d24f..ff596cf9).
- **4.2 Autoteste** (e2e 1712, 1714, 1715): `integration_test/demo_real_test.dart` entra nos 3 perfis demo, abre todos os mosaicos e ecrãs com mapa, e falha se algum rebentar. CI: job `autoteste` novo em `build_android.yml` (emulador api 34 google_apis_playstore, APK de teste, `pm grant`, `flutter drive`), e o `build` só corre se ele passar. Ao reler o job antes do push apanhei um buraco no portão: o drive ia por pipe para o tee e o pipe esconde o código de saída (um teste vermelho passaria a verde) — corrigido (commit `ci(autoteste)`), e o passo passa a imprimir o número de capturas e de falhas suaves. Nota honesta: o arnês só falha a sério em crash ou percurso partido; os ecrãs opcionais (chat, bloqueio, apagar conta, Goola, barbearia) tiram uma captura `zz-falha-*` e seguem — contam-se, não bloqueiam. Local, na árvore unida com a produção remota (HEAD 24e5a737): analyze 0 erros (219 avisos pré-existentes), **501/501 testes verdes** (os 5 do Codex entraram); corrida no emulador — **ESTADO FINAL: ver anexo A no fim**.
- **4.3 Push** — **ESTADO FINAL: ver anexo A no fim**.
- **4.4 `/baixar`** (e2e 1708, 1710): detecta iPhone/Android; no iPhone o botão da App Store (id 6809954739) passa a primeiro e o da Play esconde-se; no computador aparecem as duas lojas + site; QR das duas lojas (`qr-appstore.png` novo, correcção M); código BEMVINDO mantido. Publicado por `wrangler` (deploy 43ab2a96), não por push; verificado no ar (md5 do URL limpo = cache-busted; `qr-appstore.png` HTTP 200). A loja BR está aberta (`https://apps.apple.com/br/app/bora-entregas-e-servi%C3%A7os/id6809954739`); PT/US dão 404 até declarares o estatuto de comerciante.
- **4.5** `ios/LANCAMENTO-IOS-ESTADO.md` ganhou a secção "5. PÓS-APROVAÇÃO" (aprovada 12/09, build 115, BR ao ar, PT travado, renovação MANUAL em 09/2027). O Flutter lê `ios_hide_nonpartner_logos` em `lib/config/ios_launch_flags.dart` (carregada em `main.dart` no arranque e após login).

### Bloco 5 — skill `protocolo-missao-bora` (feito no tempo morto do build do APK)
- Criada `.claude/skills/protocolo-missao-bora/SKILL.md` (frontmatter com telemetria): as três portas de 13/09 alinhadas com a página `sistema-motores-e-portas` da `claude_ai_memoria` (Claude Code só Claude; OpenCode = ChatGPT Plus + Go + Zen + Ollama; navegador = Gemini/ChatGPT web), divisão por motor, MCP-first, formato do prompt de missão, carta de autonomia, lei do pré-voo, observabilidade no `e2e_log`, digest, painel admin, idiomas, CI/build, Bloco F e modelo de prompt curto.
- Ligada no `CLAUDE.md` (secção "Skill Usage Rule") e na skill `ceo-ai` (secção 8, workflow).
- Copiada para `guarda-fc-site` e `BoraStudio` (`.claude/skills/protocolo-missao-bora/SKILL.md`, md5 igual 7d9477cb): nenhuma sessão do Claude Code estava aberta nesses repos (nenhum registo de sessão modificado nos últimos 45 min).
- Auditoria do `PADRAO_BORA.md` contra as 4 regras de 10/09: **cartão já provado não reprova** já existia (1.17); **prova sempre pela web** já existia (3.10) e ganhou o parágrafo do autoteste no emulador (13/09); **pedido mínimo / taxa pequena** faltava — acrescentada em 2.3 com a cicatriz de 12/09 (orçamento sem linhas do carrinho, commits f25fc34b e 210a1182); **loja fechada não bloqueia ao clique** faltava — nova 1.27 com a cicatriz de 27/08 (comentário "LOJA FECHADA E VISITAVEL" em `restaurants_screen.dart`; travão só no carrinho e no gatilho `STORE_CLOSED`).

### Bloco F — este ficheiro; cópia em `C:\Users\danil\Desktop\Bora\prompts\`; digest em `claude_ai_memoria`; `e2e_log` `fim`; Telegram; `/ctx doctor` + `/ctx stats`.

## 4. Bugs reais apanhados na auditoria (nenhum era conhecido)
1. **Cliente com crédito de 5 € não conseguia pedir corrida** (FK do crédito marcada antes de a corrida existir) — corrigido, provado.
2. **Nenhum pedido das contas demo nascia** desde 12/09 (guarda nova exigia `driver_id`) — a revisão da Apple e as capturas dependem disto — corrigido, provado.
3. **Talão não-parceiro não fechava** (PATCH financeiro do lado do estafeta) — corrigido com função única, provado com pedido real.
4. **Tarifa da Vina** cobrava +1 €/km acima de 10 km — substituída pela regra fixa.

## 5. PARA O DANILO (só o que só tu podes fazer)
1. **App Store Connect → estatuto de comerciante (DSA)** — **já está submetido, está nas mãos da Apple.** Abriste a conta e eu li a página Negócios → Acordos → Conformidade: "Regulamento Serviços Digitais, 27 países (inclui Portugal), última actualização 8/09/2026, estado **Em revisão**". Não há nada para carregares; a loja portuguesa abre quando a Apple terminar a revisão. No Gmail da Bora só há um e-mail da Apple sobre isto, de 08/09 ("We received your trader contact information… we'll verify and get back to you soon") — a Apple ainda não pediu mais nada. Se pedir para confirmar telefone ou morada, é só responder a esse e-mail. Depois de PT abrir, tiro do `/baixar` a frase "se a App Store disser que ainda não está disponível…". Nota à parte, sem urgência: o "Acordo de apps pagas" está em "informações pendentes" (falta conta bancária e o formulário fiscal dos EUA) — a Bora é gratuita, isto não a trava; só interessa se um dia quiseres cobrar pela app dentro da App Store.
2. **Renovação da conta Apple Developer é MANUAL em 09/2027** (caixa de renovação automática desmarcada). Está em `ios/LANCAMENTO-IOS-ESTADO.md`; se quiseres, marco no calendário.
3. **Mensagem à Vina** (podes copiar): "Olá Vina, o plano antigo cobria 10 km e por isso aparecia +2 € em trajectos maiores. Cancelámos esse plano. A partir de agora, qualquer viagem até 15 km fica a 5,00 € fixos; acima de 15 km aplica-se o preço normal. Obrigado pela paciência."
4. Se quiseres reverter alguma das migrações de dinheiro do §2, diz qual.

## 6. Ficheiros desta janela
- Migrações novas (6): `supabase/migrations/20260913193128_*`, `…193234_*`, `…193545_*`, `…193756_*`, `…194352_*`, `…195205_*`. Espelhos (17): `20260909160856` … `20260913182921`.
- Flutter: `lib/services/talao_nao_parceiro_service.dart` (novo), `lib/services/navigation_service.dart`, `lib/screens/driver_map_screen.dart`, `lib/screens/admin/admin_receipts_screen.dart`, `lib/screens/admin/admin_platform_settings_screen.dart`, `lib/screens/admin/admin_order_detail_screen.dart`, `lib/main.dart` (união do merge).
- CI: `.github/workflows/build_android.yml` (job `autoteste`).
- Docs: `ios/LANCAMENTO-IOS-ESTADO.md`.
- bora-site: `baixar.html`, `assets/img/qr-appstore.png` (commit local b36887d; publicado por wrangler).
- Provas: `.claude/.ai/provas/fable-13-09/` (analyze/test logs, diff da timeline, `emulador/` com os scripts e logs do autoteste).
- Quarentena: `C:\Users\danil\_QUARENTENA\2026-09-13\fable-13-09\` (5 espelhos errados, 1 teste alheio, `index.lock`).
- Memória do Claude Code: `trava-le-comandos-como-ddl-e-stash-push`, `contas-demo-e-gatilho-caixa-fechada`, `ramo-ios-duas-linhas-e-app-store-pt`, `prova-em-rollback-por-do-raise`, `autoteste-android-apk-e-permissoes`.

## 7. Continuação das 22:31 — ordem extra recebida a meio do Bloco 4
A ordem de continuação dizia que o PC tinha morrido às 22:00 e que o push nunca tinha saído. Foi escrita com o rasto só até à linha 1721; o estado real medido às 22:31 (linha 1727) era outro: o push saíra às 22:20 (origin = HEAD = ff596cf9), o CI #430 estava a correr o autoteste no emulador do runner antes do build, o versionCode continuava 603, o build local estava a compilar Java, e o stash `alheias-fable-13-09` estava intacto. Não recomecei nada. Como o push já tinha saído com o portão do CI à frente do build, o ramo `candidata-2026-09-13` não foi necessário para esta publicação; fica como regra para qualquer correcção seguinte (nunca publicar vermelho).
- **Regra nova LOGIN NO NAVEGADOR** (texto literal do Danilo) gravada em `.claude/skills/protocolo-missao-bora/SKILL.md` e no `CLAUDE.md` do repo (linha 1729). Já foi assim nesta sessão: App Store Connect e GitHub lidos pela sessão existente no teu Chrome, sem senhas nem tokens.
- **Patch do consolidador** aplicado (`git apply` limpo, 60 linhas a mais / 7 a menos) e o consolidador corrido uma vez: digest de 12.968 bytes com a secção "0. MEMÓRIA DA CLAUDE.AI" (4 páginas: regras-do-danilo, estado-2026-09-13 e os 3 últimos digests). Canário provado: a frase "regras do danilo" aparece na saída do injector de arranque (1 vez, 15.146 bytes); o `injecao-digest.log` regista a injecção (OK bytes=15145), porque esse log só guarda tamanhos (linha 1730).
- **Ramo `empresa-agentes-2026-09-07`**: são 8 commits (07 e 08/09), não 4; testado com `git merge-tree` sem tocar na árvore — conflito em 8 ficheiros do iOS (`build_ios.yml`, `Info.plist`, `project.pbxproj`, `ios_launch_flags.dart` e 4 documentos do iOS), porque o ramo carrega uma versão paralela e mais antiga do lançamento iOS. Não se forçou; não entra no push (linha 1728). Nota: esse ramo também traz uma versão de 443 linhas da Edge `delete-account` e o painel `/admin/motores` — fica para uma ordem própria, partindo da produção unida.

### 7.1 Adendo do Danilo (blocos 2, 3 e 5 do prompt das portas) — onde vive a conta Plus
Sem sair de nenhuma conta nem pedir login (linha 1734 do e2e_log): a conta **ChatGPT Plus é `nilofulfaro@gmail.com`** e vive no **aplicativo do ChatGPT instalado no Windows** (processo `ChatGPT.exe` a correr, sem janela visível) e no **Codex CLI** (`codex login status` diz "Logged in using ChatGPT"; o `auth.json` do Codex diz `chatgpt_plan_type=plus`, renovado a 12/09). Nos navegadores: Chrome perfil "Danilo" = `nilofulfarotuga@gmail.com`, perfil "Bora" = `boraappbora@gmail.com` (ChatGPT Free); Edge = `danilofulfaro@hotmail.com`. Nenhum perfil do Chrome tem o Google da conta Plus iniciado. A regra do login no navegador ficou reescrita como **último recurso** na skill `protocolo-missao-bora` e no `CLAUDE.md`, com este mapa, para não se voltar a procurar. O Modo de programador, os conectores e as imagens do Bloco 5 das portas são trabalho da janela das portas, não desta; ficou o caminho escrito (Definições da conta Plus no app, pelo agente de clique).

## Anexo A — estado final do autoteste local e do push (preenchido no fecho)

**O que viaja no push** (medido depois do merge de correcção, `origin/autonomous-night-2026-04-29` → HEAD 24e5a737): 110 commits, 99 ficheiros, 11.633 linhas a mais e 706 a menos. Por pasta: 23 migrações (17 espelhos + 6 novas), 17 ecrãs, 7 serviços, 4 do iOS (`Runner`), 3 ferramentas iOS, 2 workflows, 2 skills, 2 testes de integração, 3 ficheiros em `.claude/.ai`. Quase tudo é a linha do `origin/ios-lancamento` (a build 115 aprovada) mais os commits desta janela. Zonas da Trava tocadas em relação à produção: **nenhuma** (`order_store.dart` fica exactamente igual à produção). `pubspec.yaml` está em 1.0.1+603 como a produção; o CI passa a 604.

**Push feito** às 22:20 (21:20:53Z): `41f6d24f..ff596cf9`, e o `git ls-remote` devolveu `ff596cf9…` igual ao HEAD local (linha 1722 do e2e_log). Sem force. A publicação no Play só acontece se o job `autoteste` do CI ficar verde, porque o `build` depende dele — é essa a garantia do "sem verde, nada é publicado".

**CI (lido no GitHub pelo teu Chrome, porque não há `gh` no PC):** o commit ff596cf disparou três runs. A web (`build_web_deploy` #125) e o `olho-golden` #115 ficaram verdes em 2 min 42 s e 1 min 03 s — ou seja, a web já está publicada com a árvore unida — e medi o efeito, não o invólucro: o `main.dart.js` servido em `app.boraguarda.com` (9,7 MB, pedido com cache-busting) contém os nomes das funções novas desta janela (talão do não-parceiro ×3, registo de erro de ciclo, correcção de talão pelo admin) e o `driver_accept_offer` do Codex (linha 1726 do e2e_log). O Android (#430, run 34783547286) arrancou a correr o job `autoteste` no emulador antes do build.

**Run #430 ficou VERMELHA no job `autoteste` (15 min 53 s) e o build não correu — o portão fez o que devia: nada foi publicado (versionCode continuou 603, linha 1735).** A causa não foi a app: o `android-emulator-runner` corre cada linha do `script:` num `sh -c` separado, a variável com o caminho do APK chegou vazia ao `adb install` ("filename doesn't end .apk") e o passo morreu antes do teste (linha 1736). Correcção: toda a lógica passou para `tool/ci/autoteste_emulador.sh`, chamado numa linha só; o workflow aceita também o ramo `candidata-2026-09-13` (só autoteste) e o build fica preso ao ramo de produção por `if:`. A ordem pedia o ramo candidata, mas o guardrail de git deste PC só deixa empurrar o ramo de produção ("é o Danilo que o corre à mão") e os hooks mandam (linha 1737); como o build já depende do autoteste verde, a garantia é a mesma. Segundo push: `fbe83b61..156c513c` às 21:48Z (linha 1738), com mais um espelho horário do Córtex pelo meio.

**Autoteste local (a única tentativa que a ordem permitia, e mais uma):** o APK de teste compilou às 22:43 (167 MB) depois de três obstáculos de ferramenta que não são da app — Java 25 (resolvido com JDK 17 portátil), Flutter 3.47 vs AGP 8.9.1 (validação saltada) e o descarregador do NDK do Gradle encravado nos 51 MB (NDK descarregado e instalado à mão). Instalou no emulador (Success), mas o `flutter drive` deixou a app parada no ecrã de arranque: a app é lançada em pausa e o drive nunca se ligou ao serviço da VM (sem linha "Dart VM service is listening" no logcat, sem `adb forward`, processo do drive a 0 de CPU), com a rede do emulador a funcionar (ping e DNS do Supabase respondem). Terminei-o e fiz uma segunda e última tentativa com tecto de 15 minutos (linha 1739) — resultado no fim deste anexo. É um problema do acoplamento `flutter drive` ↔ emulador neste Windows, não da app; o portão que manda é o do CI.

**Segunda tentativa local:** igual à primeira — a app fica no ecrã de arranque, o drive nunca se liga, `timeout` aos 15 min (DRIVE_EXIT=124, 0 capturas). Fica declarado: **o autoteste local neste PC não correu**; a causa está no acoplamento do `flutter drive` com o emulador no Windows (sem linha de serviço da VM no logcat), não na app.

**Run #431 (portão corrigido) também VERMELHA — mas desta vez o portão funcionou e o teste correu:** `adb install` Success, o drive ligou-se à app, o Supabase iniciou, e o teste morreu aos 22 segundos na **primeira fotografia**: "Bad state: Call convertFlutterSurfaceToImage() before taking a screenshot". É uma exigência do `integration_test` **no Android** (o arnês só tinha corrido no simulador iOS, onde isto não é preciso) — defeito do arnês, não da app (linha 1740). Correcção: converter a superfície uma vez, no Android, antes da primeira fotografia. Terceiro push a seguir.

**Terceiro push:** `156c513c..56656928` às 23:05Z (00:05 de 14/09), só com a correcção do arnês (`flutter analyze` do ficheiro: sem problemas; linha 1742). A run #432 corre o autoteste e, se verde, o build sobe o 604 ao Play.

**Run #432 — VERMELHA, mas desta vez o teste correu 7 minutos dentro da app e apanhou um problema real:** consentimento, ecrã de perfis e login do cliente demo passaram (capturas no artefacto `autoteste-android-432`, 2,7 MB); depois "nenhuma das 5 lojas abriu, e são 23:25 no simulador (UTC)". A causa não é a hora, é a app: `openRetailBusiness` (mercados e lojas) ainda tinha o portão de horário de 21/05 que faz `return` com um aviso quando a loja está fechada — os restaurantes já entravam sempre desde 27/08 (regra 1.27, ordem do Danilo de 27/08 e 10/09), os mercados não. Um teste que corre de noite descobre o que só se cumpria de dia. **Correcção na app (zona verde):** portão retirado em `lib/screens/stores_screen.dart`; entra-se sempre, o travão fica no carrinho (`CartStore.lojaFechada`, que já estava configurado) e no gatilho do servidor. Juiz: `flutter analyze` 0 erros, 501/501 testes. **Quarto push:** `56656928..207adea4` às 23:36Z (linha 1748). Cicatriz registada no PADRAO 1.27.

**Run #433 — VERMELHA, e mais um degrau subido:** a loja fechada já abriu de noite (capturas das categorias, dos mercados e da loja gravadas) e o teste morreu aos 5 min 50 s em "não apareceu: botao-ver-carrinho" — com a loja fechada, meter no carrinho é travado pelo carrinho (é a app a cumprir a regra 1.27) e o botão "Ver carrinho" nunca aparece. O arnês tinha sido escrito para corridas de dia. **Correcção no arnês:** entre as 22h e as 8h UTC, sem "Ver carrinho", fotografa e salta o carrinho e o pagamento; em horário de loja o fluxo corre inteiro (linha 1756). **Quinto push:** `f916c7be..6bd4bc25` às 00:11Z (linha 1757), com mais um espelho do Córtex pelo meio.

EM CURSO — ver actualização abaixo (resultado final da run #434 do CI).
