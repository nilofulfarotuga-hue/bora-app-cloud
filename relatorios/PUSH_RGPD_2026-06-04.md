# 🚀 PUSH RGPD — Relatório de Diagnóstico (2026-06-04)

## Status

- Push: **OK** — ramo já estava sincronizado com origin
- Commits verificados no remoto: **Sim** (5/5 RGPD commits confirmados)

## Commits remotos

Últimos 5 SHAs em `origin/autonomous-night-2026-04-29`:

| SHA | Mensagem |
|---|---|
| fbf6182 | docs: relatorio CORRECAO_FOTOS_ADMIN 2026-06-02 (RGPD + UX admin) [skip ci] |
| dcc3fd1 | feat(admin): mostra vehicle_doc_url (livrete) no admin_driver_approval |
| 49a544e | feat(admin): seccao Documentos no admin_partner_detail + PrivateBucketImage no partners_pending |
| 3d7a1af | fix(security/RGPD): upload-restaurant-asset rota owner_doc/activity_doc para bucket privado restaurant-documents |
| af3e27a | fix(security/RGPD): cria bucket restaurant-documents privado + migra 2 ficheiros sensiveis [skip ci] |

## GitHub Actions

**Nenhum novo run foi acionado.** Motivo: o tip commit `fbf6182` tem `[skip ci]` no título → workflow ignorado pelo GitHub Actions. Os commits `af3e27a` também tem `[skip ci]`. Os 3 commits elegíveis (dcc3fd1, 49a544e, 3d7a1af) já foram ultrapassados pelo `[skip ci]` no tip.

API GitHub Actions retornou 404 (repo privado sem token — esperado em ambiente local sem `gh auth`).

## Diagnóstico do problema original

O ramo local `autonomous-night-2026-04-29` **já estava totalmente sincronizado** com `origin`. O push anterior (sessão 2026-06-02) foi bem sucedido — `git log origin/autonomous-night-2026-04-29..HEAD` retorna vazio.

Razão para não aparecer build novo: o commit mais recente (`fbf6182` — relatório `[skip ci]`) foi intencionalmente marcado para não acionar CI.

## Resolução aplicada

Nenhuma acção de push necessária. Para acionar o próximo build Android:

```bash
# Opção A: bump versionCode (commit limpo sem [skip ci])
# Editar bora_app/android/app/build.gradle → versionCode +1
# git commit + push

# Opção B: dispatch manual (se gh auth configurado)
gh workflow run build_android.yml --ref autonomous-night-2026-04-29
```

## Resumo

Todos os 5 commits RGPD (correção bloqueador de lançamento) estão em `origin`. O último build oficial (build #241, 1 de Junho) não inclui estas correções — precisam de um bump `versionCode` para gerar build +242 no Play Internal.
