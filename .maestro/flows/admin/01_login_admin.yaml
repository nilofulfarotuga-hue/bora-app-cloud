appId: pt.boraapp.bora
name: Admin — Login + acesso ao painel
tags:
  - admin
  - auth
---
- launchApp:
    clearState: true
- runFlow: ../../_shared/login_admin.yaml

# Sucesso = dashboard admin visível.
- scrollUntilVisible:
    element:
      text: ".*(Settlements semanais|Pedidos|Acerto reservas parceiros|Gestão).*"
    direction: DOWN
    timeout: 10000
- assertVisible:
    text: ".*(Settlements semanais|Pedidos|Acerto reservas parceiros|Gestão).*"
